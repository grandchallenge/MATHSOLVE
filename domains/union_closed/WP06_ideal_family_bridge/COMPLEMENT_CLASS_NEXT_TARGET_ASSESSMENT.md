# Complement-Class Next Target Assessment

Date: 2026-06-21

## Result Status

| Field | Status |
|---|---|
| Result status | Selected next restricted target |
| Conditional on | A union-closed family being represented as the complement of a local ideal family |
| Strongest supported claim | The checked WP06 bridge applies directly to the class of complement families generated by `IsIdealFamilyOn F U`. |
| Not claimed | This is not a new proof for arbitrary union-closed families and not an activation of the functional-preorder branch. |
| Computation class | `CONTINUUM_PROOF`, via the existing MATHCERT Lean theorem |
| Certification state | Wrapper target only; canonical proof is `UC-WP06-L008` in MATHCERT |
| First executable step | Add the Lean wrapper theorem `complementOfLocalIdealFamily_frankl` as a one-step unpacking of `localIdealFamily_complement_frankl`. |

## Selected Output Type

This assessment selects output type **(2)** from `12_NEXT_EXECUTABLE_STEP.md`:

> a restricted class of union-closed families known to be complements of local
> ideal families.

The selected class is:

```text
G is in the WP06 complement class iff
  exists F U, IsIdealFamilyOn F U and G = complementFamilyOn F U.
```

This class is intentionally narrower than all union-closed families. It is the
largest class that the current checked WP06 theorem can consume without adding
new mathematical hypotheses.

## Exact Target Statement

The first formal target after this assessment should be the wrapper:

```lean
theorem complementOfLocalIdealFamily_frankl
    {G : Family α}
    (hG : ∃ F U, IsIdealFamilyOn F U ∧ G = complementFamilyOn F U) :
    IsUnionClosed G ∧ IsNontrivial G ∧ IsFranklAbundant G
```

This theorem is not a new proof route. It is a presentation and reuse wrapper
around:

```lean
localIdealFamily_complement_frankl
```

which is recorded canonically as `UC-WP06-L008` in MATHCERT.

## Dependency On UC-WP06-L008

The dependency is direct:

1. unpack `hG` as `⟨F, U, hIdeal, hEq⟩`;
2. rewrite `G` using `hEq`;
3. apply `localIdealFamily_complement_frankl hIdeal`.

No exact finite certificate and no new external theorem is required.

## Claim Boundary

This target may claim:

> Every family that is explicitly represented as the complement of a local
> ideal family is union-closed, nontrivial, and Frankl-abundant.

This target may not claim:

> Every union-closed family has such a representation.

The missing representation theorem is separate proof debt. Without it, this
wrapper is a reusable restricted theorem, not progress on the full conjecture.

## Non-Selected Branch: Functional Preorders

WP03 records `UC-WP03-C007`, the Hachimori-Kashiwabara functional-preorder
order-ideal branch, as a plausible later extension. It should not be activated
as a direct application of WP06.

The reason is semantic. `IsIdealFamilyOn F U` requires subset-downward closure:
if `B ∈ F`, `B ≠ U`, and `A ⊆ B`, then `A ∈ F`. An order ideal of a preorder is
downward closed in the preorder relation, not necessarily under arbitrary
subset inclusion. For example, in a two-element chain `a ≤ b`, `{a, b}` is an
order ideal, but the subset `{b}` is not an order ideal. Therefore the family
of preorder order ideals does not automatically satisfy the local ideal-family
hypotheses used by `UC-WP06-L008`.

Functional preorders remain a later branch only after one of these is supplied:

1. a semantic bridge proving the relevant order-ideal family satisfies a local
   predicate consumable by MATHCERT; or
2. a separate local theorem for preorder order ideals, with its own checked
   proof boundary.

## Source Boundary

- Hachimori-Kashiwabara 2025 proves average rarity for ideal families and
  reports a Lean 4 formalization: <https://arxiv.org/abs/2504.13454>.
- Hachimori-Kashiwabara 2025 functional-preorder work reports average rarity
  for order ideals of functional preorders with Lean 4 verification:
  <https://arxiv.org/abs/2511.19833>.

The first source aligns with the checked WP06 bridge. The second source is not
used as a direct WP06 consumer.

## Certification Route

The Lean wrapper should live in MATHCERT near `IdealFamilyDuality.lean` or in a
small follow-up file importing it. The proof should be a direct wrapper, with
no new axioms and no new external dependency.

Acceptance criteria:

- the wrapper compiles;
- `UC-WP06-L008` remains the canonical certified theorem;
- the MATHSOLVE ledger uses a presentation-layer ID, not a duplicate
  `UC-WP06-L*` ID;
- MATHSOLVE and MATHCERT validators pass.

