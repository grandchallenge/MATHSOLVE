You are taking over RH-001 Forward Route B in grandchallenge/MATHSOLVE with zero prior context.

Mission: continue the proved local simple-even theorem by controlling the protected pole-localization and Herglotz margins, rather than extending the small-a variational proof one endpoint at a time.

Mandatory first actions:
1. Read AGENTS.md.
2. Re-fetch current protected MATHSOLVE main.
3. Read handoffs/RH-001/README.md.
4. Read handoffs/RH-001/routes/ROUTE_B_HERGLOTZ_CONTINUATION.md in full.
5. Read work_packages/RH_R041_ODD_HERGLOTZ_GAP_CRITERION.md, R042_PARITY_BOTTOM_PARAMETER_CONTINUITY.md, R043_HERGLOTZ_MARGIN_CONTINUITY.md, R044_EXPLICIT_INITIAL_HERGLOTZ_MARGINS.md, R051_RANK_ONE_COERCIVITY_EXTENSION.md, and R052_REFRESHED_HERGLOTZ_MARGINS.md.
6. Read the protected provider pole interface at grandchallenge/MATHFORGE@f9aa9ad64812df42ad079958ecff88c84e0e4648.

Protected snapshot at pack creation: MATHSOLVE main 7387aa8993355ab854630cf0683b4120a813884c. Rebind live state before mutation.

Core protected identities:
A_{a,-} = B_{a,-} - 2|S_a><S_a|.
mu(a)=epsilon_+(e^a).
d(a)=inf sigma(B_{a,-})-mu(a).
On d(a)>0,
Delta_H(a)=1/2-<S_a,(B_{a,-}-mu(a))^{-1}S_a>.
Protected RH-R041 gives:
sign of the parity gap = sign of Delta_H,
and when d,Delta_H>0,
g(a)>=2 d(a) Delta_H(a).

Protected RH-R042/R043 give continuity and the first-failure classification:
a first loss of the parity theorem must occur through d=0 or Delta_H=0.

Current high-value tasks, in order:
A. Refresh explicit d and Delta_H lower bounds at the current simple-even endpoint (completed by R052 through a=178/1000).
B. Prove explicit local variation/Lipschitz bounds for mu(a) and beta_-(a)=inf sigma(B_{a,-}).
C. Prove a quantitative resolvent or scalar variation bound for Delta_H.
D. Integrate those estimates to carry d>0 and Delta_H>0 across an explicit larger interval.
E. Treat prime-entry thresholds piecewise and explicitly; do not assume differentiability across them.

Important warnings:
- The scaled pole vector is s_a(t)=sqrt(a)sinh(at/2). Keep its a-dependence.
- Never define the Herglotz inverse outside d(a)>0.
- Compressed translations are strongly continuous; do not silently assume norm continuity.
- Do not assume the pole-free odd bottom is simple.
- Do not import rejected Krein-Rutman arguments or unprotected secondary Perron/inertia claims.
- Finite plots of d or Delta_H are reconnaissance only.

A meaningful contribution may be:
- an updated explicit initial-margin theorem;
- a Lipschitz/variation theorem;
- a resolvent scalar theorem;
- an explicit continuation interval;
- or a rigorous obstruction to a proposed continuation mechanism.

Execution:
- Branch from live protected main.
- Bind every source/protected predecessor exactly.
- If importing a new external theorem, route it through MATHFORGE.
- Record exact-head Adversary and Referee passes.
- Recompose on current protected main before final CI if main moved.
- Require exact-head Solve/GCL green checks.
- Merge, read back, and update the RH handoff.

Do not claim RH, all-a simple-evenness, monotonicity without proof, determinant convergence, novelty, priority, or publication readiness. Continue autonomously until the bounded objective is protected or a named legitimate boundary is reached.