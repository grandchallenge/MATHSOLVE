# BSD-001 WP36 frontier

## Candidate predecessor identities

WP36 is authored from protected MATHSOLVE

`757da986335a11a847bf31b396396729e1e7e983`

and protected MATHFORGE source authority

`92fcd8596e59aa8331b2450ea66c14786581806e`.

No WP36 conclusion is protected until exact-head review, affected CI, merge, and protected-main readback complete.

## Candidate WP36 result

Protected WP31 reduces the remaining place-2 local defect to

`c_tw(P) mod 2^{m_2}`

with

`m_2=ord_2(3-a_2) in {1,2}`.

WP36 gives a terminating exact computation of that class.

For the degree-2 and degree-4 local cyclotomic layers, it proves

`Tr(O_{L_1})=2 Z_2`,

`Tr(O_{L_2})=4 Z_2`.

Combining these identities with the protected Silverman deep-logarithm interface gives

`N Ehat(4O_{L_1})=Ehat(8Z_2)`,

`N Ehat(4O_{L_2})=Ehat(16Z_2)`.

Hence the full shallow norm image is determined by finite maps whose source quotients contain only

`8`

and

`128`

classes, respectively.

Let

`Q_P=[2^{r_red(P)}]P in Ehat(2Z_2)`.

Its class is protected `z_form(P)`. The finite norm table determines:

- for `m_2=1`, whether `c_tw(P)` is `0` or `1 mod 2`;
- for `m_2=2`, whether `c_tw(P)` is `0 mod 4`, `2 mod 4`, or odd, by testing `Q_P` and `[2]Q_P` for finite-layer norm membership.

Thus `tau_form(P)`, `rho_2(P)`, `rho_E`, and the protected control-defect length `len C_E^vee` are exact finite arithmetic observables.

## Boundary closed

WP36 closes

`MISSING_P2_FINITE_TWISTED_RECIPROCITY_EXPONENT`

in the exact computable-criterion sense specified by protected WP31.

No infinite local cyclotomic-tower calculation remains necessary for D1b/control-defect evaluation.

## Remaining controlling campaign boundaries

The principal open determinant/normalization obligations are now:

- D1c `MISSING_P2_ANALYTIC_DETERMINANT_GENERATOR_AT_HEIGHT_ONE_2`;
- D2 `MISSING_P2_BOCKSTEIN_TO_WP00_NORMALIZATION`.

Protected D1a remains closed in the exact square-presentation plus specialization-defect-accounting sense established by WP35.

`BSD-R2-A1` remains `SELECTED_RESEARCH_TARGET_UNPROVED`.

MATHCERT remains the only certification authority.

## Recommended continuation

Do not reopen the WP31 infinite-tower or broad universal-norm source search.

After protected admission of WP36, return to the determinant lane. The next useful operation must either:

1. supply an integral literal-p=2 analytic determinant theorem for the selected non-CM good-ordinary surjective `E[2]` branch, including the height-one prime `(2)`; or
2. bypass the missing general main-conjecture theorem by deriving the exact rank-one leading coefficient directly and then proving the D2 Bockstein-to-WP00 normalization with every power of `2` explicit.

An away-from-`(2)` one-sided Kato divisibility alone is not sufficient.
