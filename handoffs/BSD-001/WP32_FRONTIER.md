# BSD-001 WP32 frontier

## Candidate predecessor identities

WP32 is authored from protected MATHSOLVE

`6f34bba352024d864fd83c98b0532e8c71002e24`

and protected MATHFORGE source authority

`6f5059836de4330a37f6de8baec5634f889921c4`.

No WP32 conclusion is protected until exact-head review, CI, merge, and protected-main readback complete.

## Candidate WP32 result

Burns–Macias Castillo equation (20) exposes three specifically archimedean `p=2` terms:

`cok H^0(kappa_2)`, `H^1(R,T_2E)`, and `H^2(R,T_2E)`.

Over `Q`, take the full archimedean lattice

`X'_infty=H_infty(E/Q)_2`.

Because the auxiliary Galois group is trivial, this lattice is projective. Source comparison (16) then gives

`cok H^0(kappa_2)=0`.

If

`c_infty(E)=#pi_0(E(R)) in {1,2}`,

then the candidate theorem proves

`H^1(R,T_2E) ~= pi_0(E(R))`

and

`H^2(R,T_2E) ~= H^1(R,T_2E)^vee`.

Therefore

`len_Z2 H^1(R,T_2E)
 = len_Z2 H^2(R,T_2E)
 = ord_2(c_infty(E))`.

The associated bounded archimedean comparison cone has

`Fitt(H^2)/Fitt(H^1)=Z_2`

and zero alternating `2`-adic length.

Thus the real-place `p=2` finite defect carries no net determinant valuation. This is not a canonical determinant-generator identification.

## Boundaries after candidate WP32

The bounded subproblem

`MISSING_P2_PERFECT_SELMER_ARCHIMEDEAN_VALUATION_CORRECTION`

is closed if WP32 is admitted.

Canonical obligations remain:

- D1a `MISSING_P2_PRIMITIVE_CYCLOTOMIC_PERFECT_DETERMINANT_REALIZATION`;
- D1c `MISSING_P2_ANALYTIC_DETERMINANT_GENERATOR_AT_HEIGHT_ONE_2`;
- D2 `MISSING_P2_BOCKSTEIN_TO_WP00_NORMALIZATION`;
- protected WP31 `MISSING_P2_FINITE_TWISTED_RECIPROCITY_EXPONENT`.

## Next determinant-side operation

Do not return to generic p=2 perfectness or archimedean screens.

The next determinant-side tranche should isolate the finite-place comparison between the Burns–Macias perfect complex and the protected primitive Kummer module `X_E`:

1. choose the finite local modules source-compatibly;
2. evaluate the odd bad-prime finite terms against protected WP13/WP22/WP26 component/Tamagawa data;
3. reconcile the place-2 finite local condition with protected WP21-WP31;
4. determine whether the resulting two-term perfect specialization has rank-one cokernel exactly `X_E` or a fully computed finite correction;
5. only then construct the cyclotomic deformation and compare its Bockstein.

The whole-real-locus WP00 period convention remains fixed; WP32 does not insert or remove a separate `c_infty(E)` from `Omega_E`.

## Firewall

No BSD equality, analytic determinant generator, Bockstein/regulator identity, finite-place vanishing, theorem novelty, or certification is claimed.