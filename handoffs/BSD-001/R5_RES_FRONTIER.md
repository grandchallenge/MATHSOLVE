# BSD-001 R5-RES frontier

## Operation

- Operation: `BSD-R5-RES`.
- Issue: `grandchallenge/MATHSOLVE#273`.
- Protected entering MATHSOLVE base: `36202d97e2cd96956c8a4aafa43941f6219776c5`.
- Protected provider: `grandchallenge/MATHFORGE@7da3f07248b5d8cf3d0bd1e57e9d8b7f5b78d063`.
- Parent tracker: `grandchallenge/MATHSOLVE#215`.
- Programme owner: `grandchallenge/MATHSOLVE#164`.

## Entering frontier

`MISSING_P2_RESIDUAL_CYCLOTOMIC_KATO_KOLYVAGIN_NONVANISHING`.

Protected R5-PRIM has already proved:

`R5-PRIM <=> bar(kappa_Lambda^Kato) != 0 in KS_Lambda/2`,

and has reduced the inverse-limit statement to existence of a nonzero legitimate finite cyclotomic mod-2 image.

## Source-first result

The protected R5-RES provider records show that the strongest finite analytic witness architecture is a refined Kato/Kurihara reciprocity law. The exact odd-prime source formula tracks the local torsion factor

`p^t = #E(Q_p)[p^infinity]`

and a period/Manin normalization before comparing a Kato derivative component to a Kurihara/modular-symbol quantity.

No protected source extends that integral torsion-coefficient reciprocity theorem to literal `p=2`.

## Candidate disposition

`BLOCKED`.

Exact first independent theorem boundary:

`MISSING_P2_NORMALIZED_ANOMALOUS_ORDINARY_KATO_KURIHARA_RECIPROCITY`.

The required theorem must construct the literal-2 torsion-coefficient local regulator, compute its exact integral image, retain the exact local `2^t` and period/Manin factors, and prove a finite derivative reciprocity identity compatible with protected R5-PRIM finite detection.

## Why this is the first boundary

The following are already protected and are not to be reopened:

- selected literal-2 global restriction repair;
- characteristic-two localization/core graphs and finite-singular maps;
- inverse-limit compatibility;
- selected Kato derivative and first-component compatibility;
- determinant membership;
- the height-one residual and finite-detection reduction.

The remaining obstruction is therefore local/integral explicit reciprocity, not generic Kolyvagin-system structure.

## Reopening evidence

R5-RES may be reopened by either:

1. a protected literal-2 theorem satisfying the normalized reciprocity boundary together with a nonzero normalized finite witness; or
2. a different protected literal-2 theorem proving the residual Kato/Kolyvagin class nonzero without the Kurihara route.

A raw Kurihara/modular-symbol computation without the literal-2 comparison theorem is reconnaissance only.

## Claim firewall

Do not infer or promote:

- `R5_RES_ESTABLISHED`;
- `R5_PRIM_ESTABLISHED`;
- D2d;
- `BSD-R2-A1`;
- novelty or priority;
- public certification;
- MATHCERT certification.
