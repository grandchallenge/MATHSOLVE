# BSD-001 frontier — R5-LIFT equivariant determinant replay

## Exact entering anchors

- Issue: `grandchallenge/MATHSOLVE#262`.
- Protected BSD base: `grandchallenge/MATHSOLVE@270e2c2f9dde7bd817a251c9e3fb250fff36a81b`.
- Protected source authority: `grandchallenge/MATHFORGE@de2c83c2c440ac357b183b9e1018e9f69c58e1d9`.
- Constitutional authority: `grandchallenge/INTELLECT@f042220f3bed7cb7b5069256e8f6305c850c0628`.
- Active staffing directive: `GI-STEWARD-0003`.

## Entering boundary

The parent R5-LIFT boundary is

`MISSING_P2_KATO_ZETA_FITTING_DIVISIBILITY_AT_HEIGHT_ONE_2`.

The exact operation sub-boundary is

`MISSING_P2_EQUIVARIANT_CYCLOTOMIC_DETERMINANT_STARK_KOLYVAGIN_REPLAY`.

The operation tests the reopened Burns-Kurihara-Sano / Kataoka-Sano finite equivariant determinant architecture over

`R_{m,n}=Z/2^m[Gal(Q_n/Q)]`.

## Candidate result

The candidate proves the group-ring replay rather than citing the protected base-ring WP60R-WP60T conclusions by scalar extension.

The new proof establishes:

1. the local Artinian Gorenstein coefficient structure and one-dimensional socle of every `R_{m,n}`;
2. the fixed protected primitive-unipotent `tau` quotient as free rank one over `R_{m,n}`;
3. the exact cyclotomic-layer cohomology defect: at `m>=2` there is still only one order-two defect, detected on `tau`;
4. exclusion of that defect from every actual selected modified primal and dual Selmer group by the fixed odd multiplicative local condition, while formal H3.2(iii) remains false;
5. a socle/Nakayama lift of the WP60R characteristic-two localization, dual-killing, large-product and core-graph mechanism;
6. the real-place-corrected determinant-to-Stark isomorphism over `R_{m,n}` without a canonical generator claim;
7. the Stark-to-Kolyvagin regulator isomorphism over `R_{m,n}`;
8. a literal-two equivariant Kato derivative whose first component is `z_{Q_n} mod 2^m`, compatible in `m,n`;
9. finite basicness and inverse-limit cyclotomic determinant membership;
10. retention of the genuine good-ordinary correction, whose protected Iwasawa support contains `gamma-1` and therefore disappears after localization at the height-one prime `(2)`;
11. composition with protected WP60A-A1, which converts determinant membership into the one-sided height-one `(2)` Fitting inequality.

Candidate disposition:

`CLOSED`.

If protected, retire

`MISSING_P2_EQUIVARIANT_CYCLOTOMIC_DETERMINANT_STARK_KOLYVAGIN_REPLAY`

and

`MISSING_P2_KATO_ZETA_FITTING_DIVISIBILITY_AT_HEIGHT_ONE_2`.

Record

`R5_LIFT_ESTABLISHED`.

## Next mathematical frontier if protected

The independent final R5 equality problem remains

`MISSING_P2_DETERMINANTAL_ZETA_PRIMITIVITY_AT_HEIGHT_ONE_2`.

That is `R5-PRIM`. Membership gives a one-sided valuation/Fitting inequality; it does not prove equality or that the Kato determinant preimage is a basis.

## Preserved false/unavailable interfaces

The candidate does not reinstate:

- formal higher-level BSS Hypothesis 3.2(iii);
- full BSS Hypothesis 4.7;
- BSS Hypothesis 4.7(iii);
- infinite BSS H3.

## Claim firewall

Until exact-head review, required CI, protected merge/readback and completion receipt all succeed, `R5_LIFT_ESTABLISHED` is only a candidate repository statement.

Even after protected admission, this operation does not establish R5-PRIM, D2d, `BSD-R2-A1`, novelty, priority, or MATHCERT certification.
