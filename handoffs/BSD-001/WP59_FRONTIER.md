# BSD-001 frontier after WP59 source-interface barrier

## Protected anchors

- MATHSOLVE pre-WP59 protected head: `55434c50ae05c3bdb66f329ee15d4ece4534d4fd`.
- MATHFORGE WP59 protected source-screen head: `c8af223d0e05f999b4977a0dae5b1b5281eff93e`.
- Programme owner: `grandchallenge/MATHSOLVE#164`.

This frontier becomes authoritative only after the containing WP59 candidate is protected and read back from `main`.

## Exact protected arithmetic line entering WP59

Protected WP58A gives

`delta_2(E)
 = 1 + 2 ord_2(m_K(f))
   - ord_2(c_infinity(E^D))
   - ord_2(lambda_D)
   - sum_{ell|N} ord_2(c_ell)`,

with

`lambda_D=L(E^D,1)/Omega(E^D) in Q^x`

and an explicit optimal-composite parametrization for which `ord_2(C_f)=0`.

The substantive residual is

`R_2(E,K,f)=2 ord_2(m_K(f))-ord_2(lambda_D)`.

## WP59 result

Protected MATHFORGE WP59 records a bounded September 2026 applicability screen. MATHSOLVE WP59 proves the corresponding implication-graph proposition:

> The currently protected admitted source interfaces do not entail a uniform exact value of `R_2(E,K,f)` for the selected class.

This is not a mathematical impossibility theorem and not a claim of literature exhaustiveness.

The exact source/theorem boundary is

`MISSING_LITERAL_P2_COMBINED_HEEGNER_INDEX_TWIST_LRATIO_THEOREM_WITHOUT_EXTRA_MOD2_LOG_OR_RANKZERO_SEED`.

## Why the screened routes do not currently close

1. Kato's protected literal-`p=2` interface does not control the height-one prime containing `2`.
2. Standard Heegner primitivity/main-conjecture candidates screened retain odd-prime hypotheses.
3. Kriz–Li supplies a genuine `p=2` route but requires an extra mod-`2` Heegner-log nonvanishing condition not currently implied by BSD-R2-A1 or WP09.
4. Modern exact twist-value candidates either give lower bounds or require a finite rank-zero/nonzero central-value seed; the selected base curve has `L(E,1)=0` because it has analytic rank one.
5. Special-family literal-`2` results have hypotheses not implied by the selected irreducible-`E[2]` class.

## Reopening contract

Reopen D2d immediately on any protected result supplying one of:

- `R1`: exact literal-`p=2` Heegner-index valuation for the selected class and a WP09-compatible `K`;
- `R2`: exact WP00-normalized `ord_2(lambda_D)` under all WP09 local constraints without an unavailable rank-zero seed;
- `R3`: a theorem forcing the Kriz–Li mod-`2` Heegner-log condition for some WP09-compatible `K` for every selected curve;
- `R4`: a direct exact theorem for `R_2(E,K,f)`;
- `R5`: a literal-`p=2` integral height-one-`(2)` main-conjecture/reciprocity theorem specializing to the protected determinant line.

Do not reopen merely for nonvanishing, equality after inverting `2`, an unspecified `2`-unit, or a theorem whose standing assumptions impose `p>2`.

## Parallel executable research lanes

### D1c

`MISSING_P2_ANALYTIC_DETERMINANT_GENERATOR_AT_HEIGHT_ONE_2` remains live. A genuine literal-`p=2` theorem here may satisfy `R5` and reopen D2d.

### D2a

`MISSING_P2_K_HEIGHT_NONDEGENERACY` remains live. A stronger fixed-`2` height theorem could create a combined arithmetic route and must trigger a fresh D2d replay.

### D2e

`MISSING_P2_WP06_EXACT_QUADRATIC_DESCENT_OF_NORMALIZATION` remains downstream. Do not replay WP06 as if D2d were resolved.

## Current boundary map

- D1c: `MISSING_P2_ANALYTIC_DETERMINANT_GENERATOR_AT_HEIGHT_ONE_2`.
- D2a: `MISSING_P2_K_HEIGHT_NONDEGENERACY`.
- D2b: `RESOLVED_WP52A_FINITE_COMPARISON_DETERMINANT`.
- D2c: `RESOLVED_WP54A_GLOBAL_QORD_RECONCILIATION`.
- D2d: `MISSING_LITERAL_P2_COMBINED_HEEGNER_INDEX_TWIST_LRATIO_THEOREM_WITHOUT_EXTRA_MOD2_LOG_OR_RANKZERO_SEED`.
- D2e: `MISSING_P2_WP06_EXACT_QUADRATIC_DESCENT_OF_NORMALIZATION`.
- `BSD-R2-A1 = SELECTED_RESEARCH_TARGET_UNPROVED`.

No MATHCERT certification, novelty, priority, patentability, or commercial claim is promoted.
