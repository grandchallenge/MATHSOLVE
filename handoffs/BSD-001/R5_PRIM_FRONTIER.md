# BSD-001 frontier — R5-PRIM cyclotomic height-one Kato index

## Resolved entry state

The authoritative predecessor completion receipt is

`.gcl/completions/BSD-R5-LIFT-EQUIV/COMPLETION_RECEIPT.json`.

It closes R5-LIFT and advances the campaign to

`MISSING_P2_DETERMINANTAL_ZETA_PRIMITIVITY_AT_HEIGHT_ONE_2`.

The present operation consumes protected MATHFORGE
`5aef30aa64730ca777329013e1e33c03f34a6e3a`.

## Candidate material disposition

`BLOCKED`.

The determinant-basis frontier is not left opaque. Protected R5-LIFT supplies compatible
rank-one determinant-to-Stark-to-Kolyvagin isomorphisms over the finite cyclotomic quotient
rings. Passing to the compatible cyclotomic line and localizing at `q=(2)` gives a scalar
`b_Kato in Lambda_q`, well-defined up to a unit.

With `z=aP` in the protected WP60A-A1 height-one normalization,

`M_q(kappa^Kato)=v_q(b_Kato)=v_q(a)-length_{Lambda_q}(H^2(C_q))`.

Hence

`R5-PRIM <=> M_q(kappa^Kato)=0`.

The protected localization/core-graph machinery replays the source rigidity mechanism, so the
remaining independent arithmetic equality is exactly

`MISSING_P2_KATO_KOLYVAGIN_PRIMITIVITY_INDEX_EQUALITY`.

The package does not identify this cyclotomic `Lambda_q` scalar with the different untwisted
base-level `Z_2` Kolyvagin module.

## Successor discipline

The immediate successor is a direct theorem attempt on the normalized cyclotomic height-one
Kato–Kolyvagin primitivity-index equality.

Do not open another broad source screen merely to restate the `p=2` gap. Re-enter MATHFORGE only
if the direct theorem attempt identifies a genuinely new external premise.

A modular-symbol route is permitted only if it explicitly acquires a literal-`2`
Kurihara/explicit-reciprocity bridge. The protected Castella–Sano `p>3` theorem is not such a
bridge.

## Claim boundary

`R5_PRIM_ESTABLISHED` remains false. D2d and `BSD-R2-A1` remain unproved. The raw source
normalization is not silently promoted at literal `p=2`. MATHCERT is not invoked.
