# BSD-001 frontier — R5-PRIM residual Kato/Kolyvagin nonvanishing

## Exact entering anchors

- Operation issue: `grandchallenge/MATHSOLVE#267`.
- Protected MATHSOLVE base: `grandchallenge/MATHSOLVE@50308cf34782f14fdb0421beea915311cef20bf3`.
- Protected R5-LIFT substantive merge: `6d0f94ad3227490826611473a40b92cadfbad577`.
- Protected R5-LIFT completion receipt: `.gcl/completions/BSD-R5-LIFT-EQUIV/COMPLETION_RECEIPT.json`.
- Protected source authority: `grandchallenge/MATHFORGE@5aef30aa64730ca777329013e1e33c03f34a6e3a`.
- Constitutional authority: `grandchallenge/INTELLECT@f042220f3bed7cb7b5069256e8f6305c850c0628`.
- Active staffing directive: `GI-STEWARD-0003`.

## Entering frontier

`MISSING_P2_DETERMINANTAL_ZETA_PRIMITIVITY_AT_HEIGHT_ONE_2` (`R5-PRIM`).

Protected R5-LIFT proves determinant membership. Protected WP60A-A1 says primitivity is the missing equality in the height-one `(2)` DVR.

## New proved reduction

Let

`Lambda=Z_2[[T]]`, `A=Lambda_(2)`.

The residue field of `A` is

`F_2((T))`.

The protected compatible determinant-to-Stark and Stark-to-Kolyvagin isomorphisms identify the integral Kato determinant preimage with a compatible rank-one cyclotomic Kato-derived Kolyvagin system.

The R5-PRIM condition is equivalent to nonzero reduction of that inverse-limit Kato system modulo `2`:

`bar(kappa_Lambda^Kato) != 0` in `KS_Lambda/2`.

Since

`R_{1,n}=F_2[Gamma_n] ~= F_2[[T]]/(T^(2^n))`,

and the residual inverse-limit line is `T`-adically separated, this is equivalent to:

there exists `n>=0` such that

`kappa_{1,n}^Kato != 0`.

The witness is finite-layer **nonvanishing**, not finite-layer basis status. A finite `T`-power factor becomes a unit after localization at `(2)`.

## Candidate disposition

`BLOCKED`.

Exact first independent missing theorem:

`MISSING_P2_RESIDUAL_CYCLOTOMIC_KATO_KOLYVAGIN_NONVANISHING`.

Equivalent forms under the protected reduction:

- residual inverse-limit Kato-derived Kolyvagin system nonzero;
- protected Kato determinant coordinate not divisible by `2`;
- zero excess determinant-coordinate height-one `(2)` valuation;
- some finite cyclotomic mod-`2` Kato-derived system nonzero.

## Why this is a substantive boundary

Protected WP60T constructs the literal-`2` Kato derivative and proves Fitting containments, but expressly does not prove Kato/Kolyvagin primitivity or the cyclotomic `mu` contribution.

Protected Kato source audit WP17B shows the all-height-one theorem including `(2)` assumes `p!=2`.

Protected WP17C screens the selected irreducible/surjective `S_3` residual branch and does not identify a literal-`2` uniform `mu=0` theorem there.

Protected Castella–Sano source admission at MATHFORGE `5aef30aa...` identifies determinant basis status with a normalized Kato-system divisibility index, but the theorem computing that index is stated under `p>3`. The Kurihara explicit-reciprocity route is also an odd-prime interface.

A fresh current source screen found no admitted general good-ordinary non-CM theorem covering fixed `p=2`; nearby current main-conjecture theorems continue to assume `p>2` or stronger.

Therefore the stop is arithmetic evidence, not infrastructure.

## Reopening evidence

Reopen this frontier on any one of:

1. a proof that `bar(kappa_Lambda^Kato)` is nonzero on the selected lane;
2. a proof that `kappa_{1,n}^Kato` is nonzero for some finite cyclotomic layer uniformly on the selected lane;
3. a literal-`2` normalized Kato-system divisibility-index theorem yielding the same conclusion;
4. a literal-`2` explicit-reciprocity plus refined modular-symbol theorem yielding the same conclusion;
5. an independently admitted theorem proving the exact determinant-coordinate excess `mu` is zero.

## Claim firewall

R5-LIFT remains protected. This frontier does not establish R5-PRIM, D2d, `BSD-R2-A1`, novelty/priority, or MATHCERT certification. Formal higher-level H3.2(iii), full BSS 4.7/4.7(iii), and infinite H3 remain false/unavailable.
