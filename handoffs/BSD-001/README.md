# BSD-001 native continuation handoff

## Target repository and authority

- Target repository: `grandchallenge/MATHSOLVE`.
- Current programme owner: `grandchallenge/MATHSOLVE#164`.
- Constitutional authority remains the protected GCL authority chain already bound by the campaign.
- Source/provider admission remains MATHFORGE-owned.
- Mathematical certification remains MATHCERT-only.
- `BSD-R2-A1 = SELECTED_RESEARCH_TARGET_UNPROVED`.

## Canonical continuation files

Read in this order after re-fetching protected live state:

1. `handoffs/BSD-001/README.md` — current frontier and authority map;
2. `handoffs/BSD-001/RESEARCH_PLAN_WP16_WP18.md` — approved theorem-construction programme;
3. `handoffs/BSD-001/TAKEOVER_PROMPT_WP16_WP18.md` — reusable fresh-session takeover pointer;
4. `work_packages/BSD_R2_A1_WP16A_FINITE_SELMER_STABILIZATION/` — exact finite-level stabilization;
5. `work_packages/BSD_R2_A1_WP16B_INTEGRAL_SELMER_FITTING/` — exact integral dual-Selmer/Fitting realization;
6. `work_packages/BSD_R2_A1_WP18A_RECON_COHORT/` — verified A/B reconnaissance cohort and exact local baseline;
7. protected WP06, WP07, WP09, WP12, WP13 and WP15 when composing the next step;
8. the current protected MATHFORGE BSD provider manifest and admitted WP17 source audits before any external theorem use.

Do not treat mutable issue text or conversational history as mathematical authority.

## Protected mathematical chain through WP16B

- WP05 fixes the exact target/source interfaces, removes the rational-torsion `ord_2` term, and supplies the selected rank-one/finiteness interface.
- WP06 records every quadratic `p=2` descent discrepancy and forbids identifying finite Kummer and Greenberg ordinary local conditions at `2` without proof.
- WP07 fixes the good-ordinary local-at-2 correction surface.
- WP08 isolates the cyclotomic height-one `(2)` / relative-mu defect.
- WP09 supplies an all-`2N`-split auxiliary imaginary quadratic field `K` with the admitted nonvanishing and p-adic Gross-Zagier interfaces.
- WP10 closes the mechanical odd-prime Heegner-primitivity route.
- WP11 supplies conditional `C3` control; WP12 eliminates `C3`, so the selected residual image is `GL_2(F_2) ~= S3`.
- WP13 proves the exact Tamagawa/inertia-depth dictionary and identifies residual-conductor drop with even-Tamagawa support.
- WP14 closes Chao Li applicability.
- WP15 proves finite Sha has square order and that parity/modulo-square information cannot determine the unsquared length.
- WP16A proves, for every `n>=1`,

  `s_n(E) := ord_2 #Sel_{2^n}(E/Q) - n = ord_2 #Sha(E/Q)[2^n]`,

  and

  `lim_n s_n(E) = len_Z2 Sha(E/Q)[2^infinity]`.

- WP16B fixes the primitive integral invariant. With

  `X_E := Sel_{2^infinity}^{Kum}(E/Q)^vee`,

  `L_E := (E(Q) tensor (Q_2/Z_2))^vee`,

  and

  `T_E := Tor_{Z_2}(X_E)`,

  the protected theorem gives

  `0 -> Sha(E/Q)[2^infinity]^vee -> X_E -> L_E -> 0`,

  `T_E = Sha(E/Q)[2^infinity]^vee`,

  `len_Z2 T_E = lim_n s_n(E)`,

  and

  `Fitt^0_{Z_2}(T_E) = 2^{lim_n s_n(E)} Z_2`.

Thus the selected unresolved equality is exactly

`delta_2(E) = v_2(Fitt^0_{Z_2}(T_E))`,

where

`delta_2(E) := ord_2(L'(E,1)/(Omega_E Reg_E)) - sum_{ell|N} ord_2(c_ell)`.

## WP17 source result

MATHFORGE WP17A-WP17C are protected. Their bounded source diagnosis does **not** assert theorem nonexistence.

The surviving provider debt is:

`P2_GOOD_ORDINARY_IRREDUCIBLE_S3_EXACT_MU_OR_PRIMITIVE_FITTING_CONTROL`.

The source screen established that nearby results either:

- control Kato-style ordinary characteristic ideals away from the height-one prime `(2)` while the integral all-height-one upgrade assumes `p != 2`;
- impose residual-dimension-one or odd-prime hypotheses incompatible with the selected `S3` branch;
- lie in reducible-`E[2]`, supersingular, restricted-twist, or different-motive branches.

Do not widen generic literature reconnaissance again unless a new theorem specifically addresses the surviving debt.

## WP18A — verified A/B cohort

WP18A fixes a minimal diagnostic cohort using pinned Cremona/ecdata source records and in-package integer arithmetic.

### Regime A control: `53a1`

- minimal model `[1,-1,1,0,0]`;
- conductor `53`;
- rank-one source data; trivial torsion;
- `Delta_min=-53`;
- `#E(F_2)=4`, hence `a_2=-1` and good ordinary reduction at `2`;
- bad depth `n_53=1`;
- protected WP13 gives `ord_2(c_53)=0` and residual conductor `53`.

### Regime B control: `203b1`

- minimal model `[1,1,1,0,-2]`;
- conductor `203=7*29`;
- rank-one source data; trivial torsion;
- `Delta_min=-7^2*29`;
- `#E(F_2)=4`, hence `a_2=-1` and good ordinary reduction at `2`;
- bad depths `n_7=2`, `n_29=1`;
- protected WP13 gives `ord_2(c_7)=1`, `ord_2(c_29)=0`, even-Tamagawa support `{7}`, and residual conductor `29`.

Trivial rational torsion excludes rational `2`-torsion, hence `E[2]` is irreducible; protected WP12 then supplies residual surjectivity for both controls.

WP18A also records a recovery correction: the legacy compact Cremona BSD file's approximate `rational factor` column is **not** a Tamagawa product. No protected cohort conclusion uses that mistaken exploratory interpretation.

## Exact local-condition contract

The governing invariant remains the primitive classical Kummer condition at every place and finite level:

`im(E(Q_v)/2^n E(Q_v) -> H^1(Q_v,E[2^n]))`.

The following substitutions remain forbidden without an exact comparison theorem:

- Kummer at `2` -> Greenberg/ordinary connected-etale;
- primitive bad-prime Kummer -> unramified/strict/relaxed/imprimitive;
- saturated rank-one quotient -> span of a possibly nonprimitive generator;
- `Fitt^0(T_E)` -> `Fitt^0(X_E)`.

Every finite kernel, cokernel, index, determinant, Tamagawa factor, or local correction must retain its complete `2`-adic length.

## Current exact frontier

The mathematical theorem frontier remains

`BSD-R2-A1-S3-K-INTEGRAL-FITTING-CONTROL`,

with source debt

`P2_GOOD_ORDINARY_IRREDUCIBLE_S3_EXACT_MU_OR_PRIMITIVE_FITTING_CONTROL`.

The active diagnostic frontier is now

`BSD-R2-A1-WP18B-FINITE-SEL2N-MEASUREMENT`.

Its named operational boundary is

`MISSING_INDEPENDENT_FINITE_2N_SELMER_DATA_ON_VERIFIED_A_B_COHORT`.

This is a computation/evidence obligation, not a theorem-nonexistence claim.

## Immediate next executable tranche — WP18B

For `53a1` and `203b1`, obtain reproducible finite-level data independently of BSD:

- `#Sel_{2^n}(E/Q)` for increasing `n`, or an exactly equivalent finite-level representation;
- `s_n(E)=ord_2 #Sel_{2^n}-n`;
- observed stabilization index;
- independently computed Sha information only where available without BSD;
- Cassels-Tate data where computationally available;
- later, `delta_2(E)` under the fixed WP00 normalization and `epsilon_n(E)=delta_2(E)-s_n(E)` with analytic precision/provenance separated from exact arithmetic.

Do not derive `s_n` from analytic Sha. If a computational package's algorithm is relied upon beyond routine execution, source-audit its mathematical correctness interface before using it as governed evidence.

## Claim firewall

- `BSD-R2-A1` remains unproved.
- WP16A/B are representation theorems, not the analytic-to-arithmetic equality.
- WP18 is diagnostic only.
- Numerical stabilization is observation, not proof.
- Analytic Sha is not finite-Selmer evidence.
- Parity/modulo-square control is not exact length.
- “Up to a unit” may not hide a power of `2`.
- Kummer and Greenberg local conditions remain distinct until exactly compared.
- Restricted-family or all-Tamagawa-odd results do not become uniform selected-class results.
- Source admission is not MATHCERT certification.
- No novelty, priority, patentability, or commercial claim follows.

## Execution and continuity

Proceed autonomously through bounded proof, computation, falsification, exact-source admission when needed, exact-head non-authoring/read-only Adversary and Referee review, affected CI, protected merge, protected readback, and issue/handoff maintenance.

Recoverable tooling, connector, CI, formatting, logging, source-access, or computational-environment failures are not stopping conditions. Apply the recovery ladder.

Stop only for a genuine theorem/source/authority/authentication/safety/material-state/evidentiary boundary, target or normalization drift, or MATHCERT certification authority. Before stopping, name the exact boundary.
