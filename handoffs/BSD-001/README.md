# BSD-001 native continuation handoff

## Target repository and authority

- Target repository: `grandchallenge/MATHSOLVE`.
- Current ownership issue: `grandchallenge/MATHSOLVE#147`.
- Constitutional authority remains the protected GCL authority chain already bound by the campaign.
- Mathematical certification remains MATHCERT-only.

## Protected mathematical state before this candidate

Historical WP00-WP04 remain Programme-owned. Protected native Solve work contains:

- `BSD-R2-A1-WP05-SOURCE-INTERFACE-001`: exact target reconstruction and torsion-valuation removal;
- `BSD-R2-A1-WP06-2DESCENT-CONTROL`: exact integral quadratic `2^n` Selmer restriction/corestriction control;
- `BSD-R2-A1-WP07-2RECIPROCITY-LENGTH`: forced local-at-2 corrections and non-distinguished route audit;
- `BSD-R2-A1-WP08-2MU-FIREWALL`: exact height-one `(2)` / Iwasawa-mu defect theorem.

The protected Solve baseline is `e43e40e8ee7585149af7935712166ac8bf684584`.

`BSD-R2-A1 = SELECTED_RESEARCH_TARGET_UNPROVED`.

## WP09 candidate result

`BSD-R2-A1-WP09-DISEGNI-APPLICABILITY` establishes, subject to ordinary protected review and merge:

1. an admitted auxiliary imaginary quadratic `K` exists with `2` and every `ell|N` split and `L(E^D,1) != 0`;
2. `E/K` has analytic rank exactly one;
3. corrected Disegni Theorem B is applicable at `p=2` to the weight-two modular representation with trivial Hecke character;
4. the resulting exact interface is a source-normalized p-adic height / p-adic-L-derivative identity with explicit interpolation and test-vector factors.

Protected provider authority is:

- MATHFORGE `d287f457537540a69eb9446bd71567db5e334fbd`;
- MATH-PROGRAMME provider-import readback `b9219009c37ab12077a13f10dead8276836ad28b`.

This handoff does not treat WP09 as protected until its own Solve PR merges and is read back.

## Closed deductions

WP05 removes the rational-torsion `ord_2` term.

WP06 shows quadratic descent loses no unrecorded power of `2`; every discrepancy is an explicit exponent-2 defect group.

WP07 proves good ordinary at `2` forces `a_2=+/-1`, computes `ord_2(1-alpha^(-1))=ord_2(3-a_2)`, and proves standard residual 2-distinguishedness is impossible.

WP08 proves that equality of cyclotomic height-one data away from `(2)` leaves exactly a relative factor `2^delta`; after removing a common augmentation zero this shifts the first nonzero coefficient by exactly `delta`. For characteristic elements, `delta` is the relative mu exponent.

WP09 closes the previously open auxiliary-K existence and corrected p=2 p-adic Gross-Zagier applicability questions for a suitable all-split Heegner lane. It does not identify p-adic and real heights or prove the BSD leading term.

## Current frontier

`BSD-R2-A1-2PRIMARY-HEEGNER-INDEX`.

The preferred next lane is an exact direct arithmetic composition:

1. fully normalized complex Gross-Zagier for the same auxiliary Heegner class;
2. exact integral 2-primary Kolyvagin/Heegner-index or equivalent Sha-Tamagawa length theorem;
3. WP06-controlled descent back to `Q`;
4. complete WP00 normalization reconciliation.

If this lane succeeds, it may bypass the independent WP08 relative-mu defect. If it fails, the Iwasawa alternative still requires both integral `(2)`/mu control and exact p-adic-to-arithmetic leading-term comparison.

## Source boundary

MATHFORGE BSD provider coverage remains retrospective but now contains two qualified native source audits: corrected Disegni p-adic Gross-Zagier and Friedberg-Hoffstein prescribed-local auxiliary quadratic nonvanishing. Further exact Heegner-index or complex-normalization theorem premises require Forge admission or an explicit governed waiver.

External reconnaissance is diagnostic only. Search failure is not proof of nonexistence.

## Certification boundary

MATHCERT remains pending. WP09 does not alter the certification packet.

## Stop conditions

Stop for target or hypothesis drift, reliance on an unadmitted theorem as a proof premise, genuinely new integral p=2 Heegner-index/main-conjecture mathematics not established in-package, or MATHCERT certification authority. Routine bounded proof, exact falsification, review, and protected integration remain delegated.