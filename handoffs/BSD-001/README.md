# BSD-001 native continuation handoff

## Target repository and authority

- Target repository: `grandchallenge/MATHSOLVE`.
- Current native theorem-development issue: `grandchallenge/MATHSOLVE#138`.
- Constitutional authority remains the protected GCL authority chain already bound by the campaign.
- Mathematical certification remains MATHCERT-only.

## Protected mathematical state

Historical WP00-WP04 remain Programme-owned. Native Solve work now contains:

- `BSD-R2-A1-WP05-SOURCE-INTERFACE-001`: exact target reconstruction and localization of the missing integral-at-2 bridge;
- `BSD-R2-A1-WP06-2DESCENT-CONTROL`: exact integral quadratic `2^n` Selmer restriction/corestriction control.

`BSD-R2-A1 = SELECTED_RESEARCH_TARGET_UNPROVED`.

## WP06 material result

Irreducibility of `E[2]` prevents every quadratic extension from creating 2-power torsion. For every `n>=1`, restriction therefore identifies the base and quadratic-twist cohomology with the genuine plus and minus eigenspaces over the auxiliary quadratic field.

The remaining finite-level discrepancy is recorded by explicit local exponent-2 descent groups and by two exact integral splitting defects `I_n` and `Q_n`. This yields an exact finite-length Selmer identity, and under finiteness of the relevant Tate-Shafarevich groups, an exact large-`n` 2-primary Sha-length identity.

No `(1 +/- tau)/2` projector is used.

## Local-at-2 boundary

The ordinary local condition is defined from the connected-etale sequence of the good-ordinary 2-divisible group. If the auxiliary field is chosen with every prime dividing `2N` split, the local descent defect at `2` and all bad primes is exactly zero by split-local transport.

## Current next target

`BSD-R2-A1-2RECIPROCITY-LENGTH`.

Construct the integral analytic-to-arithmetic bridge at `2`: an ordinary Iwasawa/Euler-system finite-length identity plus explicit reciprocity/Gross-Zagier/height comparison, with every period, Tamagawa, Euler, isogeny, and interpolation correction retained. A direct-over-`Q` theorem remains an admissible alternative to the auxiliary-quadratic lane.

## Source boundary

Current external literature used to test the descent theorem is concordance until admitted through the governing provider route. It may not silently strengthen the BSD theorem interface.

## Certification boundary

The MATHCERT route remains pending. WP06 does not advance or modify the certification packet.

## Stop conditions

Stop for target or hypothesis drift, reliance on an unadmitted theorem as a proof premise, a genuinely new deep reciprocity/Iwasawa theorem not established in the package, or MATHCERT certification authority. Routine bounded theorem development, adversarial checking, review, and protected integration remain delegated.