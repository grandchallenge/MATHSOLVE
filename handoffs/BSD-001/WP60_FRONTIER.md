# BSD-001 frontier — WP60 literal-p=2 research programme

## Protected issuance anchors

- Constitutional authority: `grandchallenge/INTELLECT@fc9ee5537bf07586dffcc621e753204ecd835365`.
- Protected MATHSOLVE entering head: `aae4320432a5510146a5af0bebcfd966e36d7419`.
- Protected MATHFORGE entering head: `c8af223d0e05f999b4977a0dae5b1b5281eff93e`.
- Parent programme owner: `grandchallenge/MATHSOLVE#164`.
- WP60 tracker: `grandchallenge/MATHSOLVE#215`.

These SHAs are issuance anchors only. Every executing agent must resolve current protected live state before mutation.

## Exact entering mathematical state

Protected WP58A gives

`delta_2(E)
 = 1 + 2 ord_2(m_K(f))
   - ord_2(c_infinity(E^D))
   - ord_2(lambda_D)
   - sum_{ell|N} ord_2(c_ell)`,

with

`lambda_D=L(E^D,1)/Omega(E^D) in Q^x`.

Protected WP59 isolates

`R_2(E,K,f)=2 ord_2(m_K(f))-ord_2(lambda_D)`

and records the named D2d boundary

`MISSING_LITERAL_P2_COMBINED_HEEGNER_INDEX_TWIST_LRATIO_THEOREM_WITHOUT_EXTRA_MOD2_LOG_OR_RANKZERO_SEED`.

`BSD-R2-A1` remains `SELECTED_RESEARCH_TARGET_UNPROVED`.

## Approved WP60 execution programme

### WP60A — primary

Construct an exact finite-level literal-`p=2` analytic determinant generator and identify it with the protected primitive determinant/Fitting line at height one `(2)`.

Primary reopening target: WP59 `R5`.

Secondary success: direct determination of `R_2(E,K,f)` as `R4`.

Do not route through an odd-prime-only integral main-conjecture clause or hide the height-one `(2)` exponent in an unspecified unit.

### WP60B — parallel

Translate the exact Kriz–Li literal-`p=2` logarithmic condition into the protected WP00/WP09/WP38 normalization and determine whether it contains genuinely auxiliary-field-varying information independent of the unknown Heegner-index parity.

If independent information remains, seek a theorem forcing the condition for a WP09-compatible `K`.

Primary reopening target: WP59 `R3`.

Secondary success: exact `R1` Heegner-index control.

If the condition reduces to the unknown parity of `m_K(f)`, record that equivalence and terminate this route as non-independent rather than cycling.

### WP60C — supporting

Run exact theorem-discovery reconnaissance on real selected curves and real WP09-compatible fields. Generate deterministic machine-readable data for the translated mod-2 Heegner-log condition and correlated protected local/Selmer invariants.

No computational sample promotes a uniform theorem. Its role is falsification, structure discovery, and selection between proof hypotheses.

## Immediate Codex read order

After resolving live protected state, read:

1. `AGENTS.md`;
2. `handoffs/BSD-001/README.md`;
3. this file;
4. `work_packages/BSD_R2_A1_WP60_P2_FRONTIER_RESEARCH_PROGRAM/00_README.md`;
5. `work_packages/BSD_R2_A1_WP60_P2_FRONTIER_RESEARCH_PROGRAM/01_EXECUTION_CONTRACT.md`;
6. `work_packages/BSD_R2_A1_WP60_P2_FRONTIER_RESEARCH_PROGRAM/02_CLAIM_LEDGER.yaml`;
7. `work_packages/BSD_R2_A1_WP60_P2_FRONTIER_RESEARCH_PROGRAM/03_CODEX_TAKEOVER_PROMPT.md` when a self-contained takeover prompt is needed;
8. protected WP59 theorem/source-screen artifacts;
9. only then the deeper predecessor packages required by the chosen lane.

## Exact predecessor focus by lane

WP60A should reconstruct only the necessary determinant spine first:

- WP16A/WP16B;
- WP19;
- WP35–WP36 where finite ordinary/local control enters;
- WP39–WP52A for exact Kummer/Greenberg/strict comparisons and Bockstein/duality;
- WP53A–WP58A only when matching the analytic normalization line.

WP60B should focus first on:

- WP09 auxiliary-field constraints;
- WP10 literal-`p=2` Heegner barrier;
- WP38 free-lattice identity;
- WP56A Heegner-index normalization;
- WP58A fixed parametrization;
- WP59 source screen.

WP60C must import the exact selection predicates from protected WP05/WP09 and use the local finite arithmetic already made explicit in WP28–WP36 where relevant.

## Reopening contract

D2d reopens only on an exact protected result satisfying at least one of:

- `R1`: exact literal-`p=2` Heegner-index valuation;
- `R2`: exact WP00-normalized twist-L-ratio valuation under all WP09 constraints without an unavailable rank-zero seed;
- `R3`: theorem forcing the Kriz–Li mod-`2` Heegner-log condition for a WP09-compatible field for every selected curve;
- `R4`: direct exact theorem for `R_2(E,K,f)`;
- `R5`: literal-`p=2` integral height-one-`(2)` main-conjecture/reciprocity theorem specializing to the protected determinant line.

No other wording counts as reopening merely by resemblance.

## Live boundary map

- D1c: `MISSING_P2_ANALYTIC_DETERMINANT_GENERATOR_AT_HEIGHT_ONE_2` — active WP60A target.
- D2a: `MISSING_P2_K_HEIGHT_NONDEGENERACY` — retained parallel theorem boundary.
- D2b: `RESOLVED_WP52A_FINITE_COMPARISON_DETERMINANT`.
- D2c: `RESOLVED_WP54A_GLOBAL_QORD_RECONCILIATION`.
- D2d: `MISSING_LITERAL_P2_COMBINED_HEEGNER_INDEX_TWIST_LRATIO_THEOREM_WITHOUT_EXTRA_MOD2_LOG_OR_RANKZERO_SEED` — WP60 reopening target.
- D2e: `MISSING_P2_WP06_EXACT_QUADRATIC_DESCENT_OF_NORMALIZATION` — downstream; do not execute as resolved.

## Claim firewall

Do not promote:

- `BSD-R2-A1`;
- a finite computation to a theorem;
- the Kriz–Li condition to a uniform fact without proof;
- `m_K(f)` to odd;
- `lambda_D` to a `2`-adic unit;
- height existence to fixed-`2` nondegeneracy;
- an equality after inverting `2` to an integral equality;
- an odd-prime theorem to literal `p=2`;
- source admission or CI success to MATHCERT certification.

## Operational stopping rule

Continue through all reasonably available bounded recovery, proof, source-admission, computation, review, CI, merge, and readback actions.

Stop only at a named theorem/source/authority/authentication/safety/material-state/substantive-evidentiary boundary. State the boundary exactly before stopping.