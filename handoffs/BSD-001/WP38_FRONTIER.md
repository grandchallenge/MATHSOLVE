# BSD-001 frontier after WP38

## Protected predecessors

Before this tranche:

- MATHSOLVE: `a9b823e526059ffefb62b3d331d85f04c456a97d` — WP37;
- MATHFORGE: `5a5bf90af046e91ed13949ad8087c35e6a74d059` — protected WP38 bounded fixed-`2` nondegeneracy/derived-height source screen.

## WP38 results

### 1. D2a source screen

The closest screened routes do not close fixed-`2` height nondegeneracy on the selected branch:

- Macias Castillo–Sano 2026 assume `p` odd throughout;
- Howard's derived-height theory fixes `p>2`;
- Kriz–Li's apparent `p=2` Heegner nonvanishing branch is Eisenstein/residually reducible and incompatible with the protected surjective `E[2]`, good-at-`2` class.

This is a bounded applicability result, not a theorem-nonexistence claim.

D2a remains

`MISSING_P2_K_HEIGHT_NONDEGENERACY`.

### 2. D2b global lattice reduction

WP38 proves exactly

`E(Q)/E(Q)_tors ~= E(K)/E(K)_tors`

and hence

`E(Q) tensor Z_2 ~= E(K) tensor Z_2`.

Therefore quadratic base change introduces no hidden `2`-power index in the ambient rank-one Mordell–Weil lattice.

The surviving D2b boundary is strictly local/Selmer-complex:

`MISSING_P2_NEKOVAR_EXTENDED_TO_PRIMITIVE_KUMMER_LOCAL_INDEX`.

This does not prove a Heegner point is primitive and does not identify primitive Kummer and Greenberg/extended Selmer local conditions.

## Current exact theorem boundaries

`BSD-R2-A1` remains

`SELECTED_RESEARCH_TARGET_UNPROVED`.

### D1c

`MISSING_P2_ANALYTIC_DETERMINANT_GENERATOR_AT_HEIGHT_ONE_2`

No new source in WP38 changes the protected Kato/WP17C diagnosis.

### D2a

`MISSING_P2_K_HEIGHT_NONDEGENERACY`

Need a literal-`p=2` nondegeneracy theorem for the selected rank-one line, or an integral literal-`p=2` derived-height bypass.

### D2b

`MISSING_P2_NEKOVAR_EXTENDED_TO_PRIMITIVE_KUMMER_LOCAL_INDEX`

The global Mordell–Weil lattice index is now proved to be one. The remaining task is to compute the exact index/finite correction between source-compatible Nekovář extended/ordinary Selmer cohomology and the primitive Kummer lattice, especially at the two split places above `2` and any finite extended-cohomology terms.

### D2c

`MISSING_P2_DISEGNI_INTERPOLATION_FACTOR_VALUATIONS`

Compute exactly

`ord_2(e_{2,infinity}^{-1} Q)`

in the protected WP09 p-adic Gross–Zagier normalization and reconcile it with WP07/WP30 local unit-root factors. No factor may be called a unit without proof.

### D2d

`MISSING_P2_CLASSICAL_GROSS_ZAGIER_WP00_NORMALIZATION`

Compare the same Heegner line with the classical Gross–Zagier/Néron–Tate/WP00 normalization. A p-adic height is not a real height.

### D2e

`MISSING_P2_WP06_EXACT_QUADRATIC_DESCENT_OF_NORMALIZATION`

Descend the final normalized identity through WP06, retaining every global plus/minus overlap/quotient and twist term.

## Immediate successor

Proceed on D2b/D2c in parallel while D2a remains an explicit theorem premise.

The narrowest source query is now not generic nondegeneracy. It is:

> In Nekovář's literal-`p=2`, totally-imaginary Greenberg Selmer complex for `T_2(E)` over the protected split field `K`, what is the exact sequence comparing `H~^1_f(K,T_2(E))` with the classical primitive Kummer lattice, and what finite local/extended terms occur at the two places above `2`?

In parallel, inspect Disegni's literal normalization of `e_{2,infinity}` and `Q` only to compute their exact `2`-adic ideals/valuations for the protected all-`2N`-split test-vector packet.

Any new external theorem premise must first be admitted through MATHFORGE.

## Claim firewall

Do not promote:

- `BSD-R2-A1`;
- source non-applicability to theorem nonexistence;
- existence of a height to its nondegeneracy;
- equality of Mordell–Weil free lattices to equality of Selmer local conditions;
- primitivity of the Heegner point;
- an equality modulo units to a preferred generator;
- an odd-prime theorem to `p=2`;
- MATHCERT certification.
