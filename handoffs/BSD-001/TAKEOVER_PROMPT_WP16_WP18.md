# Durable takeover prompt — BSD-001 WP16-WP18

Copy the prompt below into a fresh agent session when handing off this programme.

---

Take over continuation of `BSD-001 — Birch-Swinnerton-Dyer selected rank-one 2-primary campaign`.

Treat this prompt only as a pointer. Do not rely on conversational history, stale summaries, or mutable issue text as mathematical authority.

Repositories:

- constitutional authority: `grandchallenge/INTELLECT`;
- coordination: `grandchallenge/MATH-PROGRAMME`;
- mathematical work: `grandchallenge/MATHSOLVE`;
- source/provider admission: `grandchallenge/MATHFORGE`;
- certification: `grandchallenge/MATHCERT`.

Current programme owner at issuance: `grandchallenge/MATHSOLVE#164`.

Before mutation:

1. re-fetch protected `main` for INTELLECT, MATH-PROGRAMME, MATHSOLVE, MATHFORGE, and MATHCERT;
2. read the currently effective constitutional/agent instructions that materially govern this work;
3. in MATHSOLVE, start at `handoffs/BSD-001/README.md`;
4. then read `handoffs/BSD-001/RESEARCH_PLAN_WP16_WP18.md` in full;
5. read the protected WP15 package and the exact prior packages referenced by the plan when a proof step depends on them;
6. read the current MATHFORGE BSD provider manifest and exact admitted source audits before using an external theorem;
7. treat issue #164 as coordination, not as authority over protected repository text.

The protected frontier at the time this handoff was created was:

`BSD-R2-A1-S3-UNSQUARED-P2-LENGTH-CONTROL`.

The named substantive boundary was:

`MISSING_UNIFORM_UNSQUARED_P2_LENGTH_CONTROL`.

The selected target remained:

`BSD-R2-A1 = SELECTED_RESEARCH_TARGET_UNPROVED`.

The governing exact equality is

`delta_2(E) = len_Z2 Sha(E/Q)[2^infinity]`,

where

`delta_2(E) := ord_2(L'(E,1)/(Omega_E Reg_E)) - sum_{ell|N} ord_2(c_ell)`.

The approved research programme changes representation of the right-hand side rather than continuing parity-level or broad literature routes:

`complex BSD defect`

`-> stabilized finite-level 2^n-Selmer length`

`-> integral Selmer-complex/Fitting/determinant invariant`

`-> integral p=2 control over the protected WP09 imaginary quadratic field K`

`-> exact quadratic descent to Q using protected WP06 discrepancy accounting`.

Proceed through the following threads.

## WP16 — finite-level Selmer reduction

First execute the smallest safe tranche, WP16A.

For `n >= 1`, define

`s_n(E) := ord_2 #Sel_{2^n}(E/Q) - n`.

Prove, from protected rank-one, odd rational torsion, finite Sha, and the Kummer sequence, that

`s_n(E) = ord_2 #Sha(E/Q)[2^n]`,

that `s_n(E)` stabilizes for sufficiently large `n`, and that

`lim_n s_n(E) = len_Z2 Sha(E/Q)[2^infinity]`.

Therefore reformulate the selected target exactly as

`delta_2(E) = lim_n s_n(E)`.

Do not use BSD to prove this reduction.

After WP16A is protected, execute WP16B: identify the precise finite/integral Selmer-complex, determinant-line, or Fitting-ideal invariant that realizes the same stabilized length. Specify every local condition and every normalization. Do not hide defects behind “up to a unit.”

## WP17 — imaginary-quadratic integral lane

Use the protected WP09 imaginary quadratic field `K` as the preferred arena for genuinely integral `p=2` Selmer-complex/Euler-system arguments.

External theorem premises must be admitted through MATHFORGE before protected use. Audit candidates against the exact applicability matrix in `RESEARCH_PLAN_WP16_WP18.md`.

Candidate reconnaissance may include modern Selmer-complex/Euler-system/Fitting machinery, including Bullach-Burns or successors, but these names are leads only. Do not assume they apply. Verify from primary sources:

- literal inclusion of `p=2`;
- coefficient-ring and self-duality hypotheses;
- ordinary local conditions at `2`;
- residual hypotheses;
- archimedean/totally-imaginary restrictions;
- Tamagawa and conductor factors;
- primitive/imprimitive normalization;
- exact strength: inclusion, divisibility, equality, primitivity, or unit class;
- whether every power of `2` is retained;
- compatibility with WP00 complex normalization and both WP13 residual-conductor regimes.

If a source is useful, open the smallest bounded MATHFORGE admission tranche, protect it through exact-head review/CI/readback, then return to MATHSOLVE.

Seek an integral equality or sufficient opposite divisibilities over `K`, then descend exactly to `Q` through the already-protected WP06 discrepancy formulas. No `2`-power may disappear in descent.

## WP18 — finite-level reconnaissance atlas

Once WP16 fixes the finite-level invariant, build a reproducible diagnostic atlas across representative selected curves from both WP13 regimes.

Track at minimum:

- `#Sel_{2^n}` and `s_n(E)` for increasing `n`;
- observed stabilization index;
- `delta_2(E)` under the fixed WP00 normalization;
- all bad-prime `ord_2(c_ell)`;
- residual-conductor drop;
- split/nonsplit multiplicative data;
- Cassels-Tate pairing information where computationally available;
- discrepancy `epsilon_n(E) := delta_2(E) - s_n(E)`;
- whether any discrepancy is already explained by WP06/WP13.

Candidate computational methods such as explicit 2-Selmer/Cassels-Tate work by Shukla-Stoll or successors are leads only and must be source-audited if their mathematics is relied on beyond routine software execution.

The atlas is observation only. It may falsify a theorem shape or nominate a missing correction. It may not certify or prove BSD.

## Decision gate

After WP16-WP18, choose the narrowest final theorem-building lane:

- if one integral Fitting/divisibility direction is proved, attack only the missing reverse direction or primitivity statement;
- if the algebraic determinant is controlled but the analytic comparison is missing, isolate an exact `p=2` explicit reciprocity law as the next theorem target;
- if the atlas reveals a repeatable correction, repair the theorem statement and replay the relevant prior deduction before proceeding.

Do not return to broad literature screening unless the exact WP16 invariant supplies a sharply formulated theorem query.

## Execution requirements

Before the first theorem mutation, post the canonical exact-head preflight required by the current governing process. Then continue autonomously through all routine bounded work:

- derivation and falsification;
- source reconnaissance;
- MATHFORGE source admission when materially required;
- exact-head Adversary and Referee passes under the standing staffing model;
- ordinary repository CI and GCL conformance;
- protected merge with exact-head binding;
- protected readback;
- owner-issue closure and handoff update.

Do not request Human Steward approval for routine authorized gates unless the live protected governing instrument materially reserves the transition.

## Claim firewall

Never promote any of the following without the exact proof and required certification route:

- `BSD-R2-A1`;
- an exact p=2 Sha/index formula;
- a p=2 Kolyvagin primitivity theorem;
- numerical stabilization as proof;
- modulo-square/parity information as exact length;
- an odd-prime theorem specialized to `p=2`;
- a restricted family theorem to the full selected class;
- novelty, priority, patentability, or commercial claims;
- MATHCERT certification.

## Continuity rule

Continue through all reasonably available recovery, diagnostic, repair, replay, source-admission, and evidence-gathering paths until either the authorized objective is completed or a genuine theorem/source/authority/authentication/safety/material-state boundary is reached.

Recoverable tooling, connector, CI, formatting, logging, or workflow failures are not stopping conditions.

Before stopping, name the boundary exactly. If you cannot state the precise theorem premise, source requirement, authority rule, authentication limit, safety constraint, or material scope change that blocks the next recovery action, continue.

Immediate first executable tranche: **WP16A finite-level `2^n`-Selmer stabilization**.

---
