# BSD-001 R5-RECIP frontier — literal-2 normalized Kato/Kurihara reciprocity

## Entering boundary

`MISSING_P2_NORMALIZED_ANOMALOUS_ORDINARY_KATO_KURIHARA_RECIPROCITY`.

Protected source authority is `grandchallenge/MATHFORGE@cd814844128167d0e4cdb48f69f9d01c6c0be883`. It separates the literal-2 problem into the local Bloch–Kato lattice/torsion-coefficient layer and the modular-element/KKS derivative layer, and admits literal-2 cyclotomic Kato explicit reciprocity without importing supersingular local formulas.

## Candidate theorem

On the selected good-ordinary lane, the shallow formal-logarithm anomaly forces exactly one formal rational 2-torsion point. If `2^t_2=#E(Q_2)[2^infinity]` and `q_2=#E(F_2)`, then

`log(E(Q_2)/tors)=(2^(t_2+1)/q_2)Z_2`

and

`exp^*(H^1_s(Q_2,T_2E))=(q_2/2^(t_2+1))Z_2`.

The optimal relative modular-symbol lattice is 2-saturated because its finite quotient is generated by rational cusp images and the protected odd isogeny transfers residual irreducibility to the optimal curve, excluding rational 2-torsion. Exact real-period geometry gives only the necessary bound

`2[a/n]^+ in Z_2`,

not raw coefficientwise integrality.

Define

`Delta_n^(2)=2^t_2 delta_tilde_n`.

Then `Delta_n^(2)` is integral, the KKS derivative computation replays after this scaling without inversion of 2, and the good-reduction Kato Euler factor satisfies

`(q_2/2)delta_tilde_n=(q_2/2^(t_2+1))Delta_n^(2)`.

Therefore the candidate proves

`xi_n(exp^*_{omega_E,I_n}(loc^s_2(kappa_n^Kato))) = u_n Delta_n^(2)`

for a unit `u_n` at every protected finite derivative level.

Candidate disposition: `CLOSED` for the reciprocity frontier only.

## Successor

`MISSING_P2_NORMALIZED_FINITE_KURIHARA_NONVANISHING_WITNESS`.

A protected index with `Delta_n^(2) != 0 mod 2`, or a different protected residual nonvanishing theorem, is still required before R5-RES can be reopened. If `t_2>=2`, the proved half-integrality bound forces `Delta_n^(2)=0 mod 2` for all `n`, so this local-regulator witness route is unavailable on that sublane.

No R5-RES, R5-PRIM, D2d, BSD-R2-A1, novelty/priority, public certification, or MATHCERT certification claim is promoted.
