# BSD-001 frontier after WP37

## Protected predecessor

MATHSOLVE protected predecessor before this tranche:

`09c90178c71cfe295d526d7a420e3f8c43f062d4` — WP36.

MATHFORGE source premise protected during WP37:

`eceaf6f0f2e10e5bd48afd1ca57f9245669202de` — qualified Nekovář literal-`p=2` totally-imaginary Bockstein-height interface.

## What WP37 establishes

Over the protected WP09 imaginary quadratic field `K`:

1. Nekovář's cyclotomic Selmer-complex Bockstein and height construction applies literally at `p=2` because `K` is totally imaginary.
2. The first cyclotomic height is the Bockstein morphism followed by Selmer-complex duality, not an analogy imported from odd `p`.
3. On a rank-one free source-compatible line, the generated height ideal in `Z_2` and its `2`-adic valuation are independent of primitive bases and cyclotomic generator.
4. Nekovář §§11.6–11.7 provide exact DVR leading-term/length control under their stated hypotheses. First-height minimality requires nondegeneracy.
5. The split choice of `K` from WP09 plus protected WP06 removes local quadratic-descent defect groups at every place dividing `2N`; the integral global plus/minus overlap/quotient terms remain and may not be divided away.

Thus the former generic question whether the preferred `K`-lane even has a literal-`p=2` Bockstein-height theory is closed.

## Current substantive frontier

`BSD-R2-A1` remains

`SELECTED_RESEARCH_TARGET_UNPROVED`.

Two independent principal theorem boundaries remain.

### D1c

`MISSING_P2_ANALYTIC_DETERMINANT_GENERATOR_AT_HEIGHT_ONE_2`

Protected facts:

- Kato controls the ordinary characteristic relation at `p=2` only away from the height-one prime containing `2`;
- the all-height-one upgrade used in the protected source explicitly assumes `p != 2`;
- WP35 gives the algebraic projective-dimension-one square presentation but no analytic generator.

A successor must produce a literal-`p=2` analytic determinant/characteristic theorem at `(2)` or an exact substitute that determines the same integral valuation.

### D2 narrowed

`MISSING_P2_K_HEIGHT_NONDEGENERACY_AND_INTEGRAL_NORMALIZATION_COMPARISON`

This consists of:

- `D2a` fixed-prime `2` nondegeneracy of the first Nekovář height on the selected rank-one line, or a literal-`p=2` derived-height bypass;
- `D2b` exact comparison between the Nekovář `K`-Selmer lattice and the protected primitive Kummer/WP20 Bockstein lattice;
- `D2c` exact `2`-adic valuations of Disegni's interpolation/test-vector factors;
- `D2d` exact reconciliation with the classical Gross–Zagier/WP00 Néron–Tate normalization without identifying p-adic and real heights;
- `D2e` exact WP06 descent of the final normalized identity, retaining all global integral plus/minus terms.

## Immediate successor query

Do not return to broad BSD literature screening.

The next source/theorem query is narrowly:

> For the selected non-CM good-ordinary irreducible `E[2]` branch at the fixed prime `2`, is there a theorem proving nondegeneracy/primitivity of the cyclotomic Nekovář height of the Heegner rank-one line over an imaginary quadratic field in which `2N` splits, or a literal-`p=2` derived-height/determinant theorem that bypasses first-height nondegeneracy while retaining an integral `Z_2` lattice?

In parallel, D1c may be screened only for literal-`p=2` height-one `(2)` main-conjecture/determinant results on this exact residual branch.

Any new external theorem premise must be admitted through MATHFORGE before use.

## Claim firewall

Do not promote:

- `BSD-R2-A1`;
- p-adic-height existence to nondegeneracy;
- a leading term modulo units to a canonical generator;
- a p-adic height to the WP00 Néron–Tate regulator;
- an odd-prime theorem to `p=2`;
- a CM/reducible/supersingular result to the selected non-CM ordinary irreducible branch;
- MATHCERT certification.
