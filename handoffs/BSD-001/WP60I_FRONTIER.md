# BSD-001 WP60I frontier — BSS H3 obstruction at literal `p=2`

## Protected entry

- INTELLECT: `fc9ee5537bf07586dffcc621e753204ecd835365`.
- MATHSOLVE entering head: `a73525fefc362aa4e1e03f006a46fd1dc3b961e8`.
- MATHFORGE: `54f1eaea24b35d4ca37e778786345c3622b6fd98`.
- Active tracker: `grandchallenge/MATHSOLVE#215`.
- WP60I operation: `grandchallenge/MATHSOLVE#225`.

## WP60I determination

The independent WP60F F2 obligation has been determined rather than merely narrowed.

For every selected semistable, odd-conductor, good-ordinary-at-`2`, irreducible-`E[2]` curve:

1. WP12 residual `S3` plus a cubic-field discriminant argument forces an odd bad prime with odd WP13 inertia depth;
2. the WP13 Tate-inertia formula then gives a primitive unipotent inside `G_{Q(mu_{2^infinity})}`, so formal BSS `(H2)` holds;
3. normality plus the primitive unipotent forces restricted mod-`4` image `SL_2(Z/4)` and full mod-`4` image `GL_2(Z/4)`;
4. an exact finite certificate proves `H^1(GL_2(Z/4),F_2^2) != 0`;
5. inflation to the full `2`-adic image proves the first BSS `(H3)` group is nonzero; Weil self-duality gives the same for the dual group.

Record:

`BSS_LITERAL_P2_SELECTED_H2_HOLDS_H3_FAILS`.

Therefore replace

`MISSING_P2_ELLIPTIC_H2_H3_VERIFICATION_OVER_F2_INFINITY`

by the selected-lane route disposition

`R5_BSS_STANDARD_HYPOTHESIS_ROUTE_BLOCKED_BY_H3_AT_LITERAL_P2`.

This is a negative applicability result for the currently screened BSS standard-hypothesis architecture. It is not a no-go theorem for all Kolyvagin/Fitting methods.

## Remaining F1 frontier

WP60I does not alter the independent F1 boundary:

`MISSING_P2_BSS_MINIMAL_CORE_THREE_TERM_RELATION_EXCLUSION_OR_REPLACEMENT_CONNECTIVITY`.

Because the standard BSS application route now fails `(H3)` on the selected lane, F1 work is no longer sufficient to reopen that route by itself. F1 remains mathematically useful for understanding literal-`2` core-vertex control and for any future theorem that weakens or bypasses `(H3)`.

## Highest-value successor

Do not spend the next tranche merely trying to verify BSS `(H3)` again for the selected lane; WP60I gives an explicit obstruction.

The highest-value R5 successors are now:

1. search for a literal-`2` Kolyvagin/Fitting theorem that does not require the BSS `(H3)` vanishing;
2. revisit the newer characteristic-`2` Nekovar-Selmer/Gorenstein machinery already screened in MATHFORGE and test whether it supplies the protected determinant/Fitting conclusion without the failed BSS hypothesis;
3. continue WP60H F1 only if it feeds a replacement control theorem whose hypotheses survive WP60I;
4. keep the direct height-one determinant routes R5-LIFT and R5-PRIM live.

## Unchanged firewall

`BSD-R2-A1` remains unproved. R1, R2, R4, R5-LIFT, R5-PRIM, D2a, D2d and D2e remain live. No MATHCERT certification follows from WP60I.
