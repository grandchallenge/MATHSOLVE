# BSD-R2-A1-WP59 — literal-p=2 residual source barrier

## Protected inputs

- MATHSOLVE protected base: `55434c50ae05c3bdb66f329ee15d4ece4534d4fd`.
- MATHFORGE protected WP59 source screen: `c8af223d0e05f999b4977a0dae5b1b5281eff93e`.
- Programme owner: `grandchallenge/MATHSOLVE#164`.
- Selected claim: `BSD-R2-A1 = SELECTED_RESEARCH_TARGET_UNPROVED`.

Protected WP58A reduces the exact analytic valuation to

`delta_2(E)
 = 1 + 2 ord_2(m_K(f))
   - ord_2(c_infinity(E^D))
   - ord_2(lambda_D)
   - sum_{ell|N} ord_2(c_ell)`,

where

`lambda_D=L(E^D,1)/Omega(E^D) in Q^x`.

The explicit real-component term and bad-prime Tamagawa valuations are not the substantive D2d source problem. The remaining theorem debt is the exact literal-`p=2` control of the Heegner-index/twist-L-ratio contribution.

## Result of the bounded source screen

Protected MATHFORGE WP59 screens the reasonably available routes through September 2026 and admits the bounded disposition

`QUALIFIED_NO_CURRENT_ADMITTED_LITERAL_P2_RESIDUAL_CLOSURE`.

This is not a theorem that the desired equality is impossible and is not a literature-exhaustiveness claim.

The screened routes fail for distinct, explicit reasons:

- Kato's protected `p=2` source interface does not control the height-one prime containing `2`;
- standard Heegner-primitivity/main-conjecture routes retain odd-prime hypotheses;
- Kriz–Li supplies a genuine literal-`2` route, but requires an additional mod-`2` Heegner-log nonvanishing hypothesis not implied by the selected class or WP09;
- exact quadratic-twist L-value results located either provide lower bounds or propagate exact valuations from a rank-zero/nonzero central-value seed, whereas the selected base curve has analytic rank one and `L(E,1)=0`;
- special-family literal-`2` theorems do not follow from the protected selected hypotheses.

## Route-barrier status

WP59 therefore records an interface theorem, not an arithmetic no-go theorem:

> From the currently protected admitted source interfaces, the exact post-WP58A D2d residual cannot be deduced uniformly for the selected class.

The named boundary is

`MISSING_LITERAL_P2_COMBINED_HEEGNER_INDEX_TWIST_LRATIO_THEOREM_WITHOUT_EXTRA_MOD2_LOG_OR_RANKZERO_SEED`.

## Reopening contract

Reopen D2d immediately if a candidate supplies any one of:

1. a literal-`p=2` Heegner-index theorem for every selected curve and a WP09-compatible auxiliary field;
2. an exact theorem for `ord_2(lambda_D)` that permits all WP09 splitting conditions without an unavailable rank-zero seed;
3. a theorem guaranteeing a WP09-compatible auxiliary field satisfying the Kriz–Li mod-`2` Heegner-log condition;
4. a direct theorem determining the combined quantity
   `2 ord_2(m_K(f)) - ord_2(lambda_D)`;
5. a new literal-`p=2` integral main-conjecture/reciprocity theorem controlling the height-one prime `(2)` and specializing to the protected determinant line.

Do not reopen merely for a theorem stated for an ordinary prime whose standing assumptions later impose `p>2`, a nonvanishing-only theorem, or an equality only after inverting `2`.

## Parallel live routes

The failure of the current D2d source lane does not close the campaign. The following independent boundaries remain legitimate research lanes:

- D1c: `MISSING_P2_ANALYTIC_DETERMINANT_GENERATOR_AT_HEIGHT_ONE_2`;
- D2a: `MISSING_P2_K_HEIGHT_NONDEGENERACY`.

A theorem resolving either may create a new combined route into D2d and should be checked against the reopening contract.

## Firewall

WP59 does not prove a mathematical impossibility, does not assert literature exhaustiveness, does not assign values to `ord_2(m_K(f))` or `ord_2(lambda_D)`, does not use rank-zero BSD for `E^D`, does not prove fixed-2 height nondegeneracy or D1c, does not perform D2e, and does not promote `BSD-R2-A1` or any MATHCERT claim.
