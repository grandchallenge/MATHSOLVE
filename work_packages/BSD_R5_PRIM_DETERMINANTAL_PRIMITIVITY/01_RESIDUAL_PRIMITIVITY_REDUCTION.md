# BSD R5-PRIM theorem — residual inverse-limit criterion at height one `(2)`

## 1. Protected setup

Let

`Lambda = Z_2[[T]]`, `T=gamma-1`,

and let

`q=(2)`, `A=Lambda_q`.

Protected BSD-R5-LIFT-EQUIV supplies a compatible family of finite determinant lines

`D_{m,n}=det^{-1}_{R_{m,n}} RΓ(O_{Q,S},T_{m,n})`,

where

`R_{m,n}=Z/2^m[Gamma_n]`,

and compatible isomorphisms

`Phi_{m,n}: D_{m,n} -> KS_1(T_{m,n})`

obtained by composing the determinant-to-Stark isomorphism with the Stark-to-Kolyvagin regulator.

It also supplies compatible Kato-derived elements

`kappa_{m,n}^{Kato} in KS_1(T_{m,n})`

and compatible determinant preimages

`xi_{m,n}^{Kato}=Phi_{m,n}^{-1}(kappa_{m,n}^{Kato})`.

Passing to the cyclotomic inverse limit gives the protected integral Kato determinant preimage `xi_Kato` used in R5-LIFT.

## 2. The inverse-limit rank-one line

Write

`D_Lambda := lim_{m,n} D_{m,n}`

with respect to coefficient reduction and cyclotomic quotient maps.

The protected global Iwasawa complex is perfect of determinant rank one, and determinant formation commutes with these finite base changes. Hence `D_Lambda` is an invertible `Lambda`-module. Since `Lambda` is local, it is free of rank one.

The compatible `Phi_{m,n}` induce an inverse-limit isomorphism

`Phi_Lambda: D_Lambda -> KS_Lambda`,

where

`KS_Lambda := lim_{m,n} KS_1(T_{m,n})`.

Thus `KS_Lambda` is also free of rank one over `Lambda` and

`Phi_Lambda(xi_Kato)=kappa_Lambda^{Kato}`,

where `kappa_Lambda^{Kato}` denotes the compatible inverse-limit Kato system.

### Lemma `BSD-R5-PRIM-LINE-001`

`D_Lambda` and `KS_Lambda` are free rank-one `Lambda` modules, and the protected Kato determinant preimage corresponds exactly to the protected compatible Kato-derived system under `Phi_Lambda`.

## 3. The residue field at `(2)`

The height-one localization is

`A=Lambda_(2)`.

Because `T notin (2)`, `T` is a unit in `A`. Therefore

`A/2A ~= Frac(F_2[[T]]) = F_2((T))`.

This point is decisive. The residue field at `(2)` is not `F_2`.

Consequently a factor `T^r` is invisible to the height-one `(2)` valuation: it is a unit in `A`, even though it is a nonunit in every finite local group ring `F_2[Gamma_n]`.

### Lemma `BSD-R5-PRIM-RESIDUE-002`

For `f in Lambda`, the following are equivalent:

1. `f` is a unit in `A`;
2. `f notin 2A`;
3. the image of `f` in `F_2((T))` is nonzero;
4. the reduction `bar f in F_2[[T]]` is nonzero.

### Proof

`A` is a DVR with maximal ideal generated by `2`, so (1) and (2) are equivalent. Reduction gives (2) iff the residue is nonzero in `A/2A=F_2((T))`. The natural map `F_2[[T]] -> F_2((T))` is injective because `F_2[[T]]` is a domain, giving (3) iff (4). QED.

## 4. Comparison corrections preserve height-one primitivity

Protected R5-LIFT retains the real-place and primitive strict/Kummer comparison corrections rather than deleting them.

At `q=(2)`:

- the real-place correction has determinant in `Z_2^x`, hence is an `A`-unit;
- the good-ordinary higher local term is supported at `(2,T)` and vanishes after localizing at `q` because `T` is a unit;
- every finite specialization/control module is annihilated by some `gamma^(2^a)-1`, whose reduction modulo `2` is a power of `T`, and therefore also vanishes after localizing at `q`.

Thus the source cyclotomic determinant line and the protected primitive height-one comparison line differ only by invertible determinant factors over `A`. Generator status at `q` is therefore preserved by the comparison.

### Lemma `BSD-R5-PRIM-COMPARE-003`

The protected R5-PRIM basis question may be tested on `D_Lambda tensor_Lambda A`: the retained real, ordinary and finite-control comparison terms cannot create or remove a factor of the uniformizer `2` after localization at `(2)`.

## 5. R5-PRIM as residual Kato-system nonvanishing

Choose a `Lambda`-basis `e` of `D_Lambda` and write

`xi_Kato = f e`, `f in Lambda`.

R5-LIFT is precisely the already-protected assertion that `xi_Kato` is integral, so such an `f` lies in `Lambda` rather than only in its fraction field.

Protected WP60A-A1 together with Lemma `COMPARE-003` identifies R5-PRIM with `xi_Kato` generating the determinant line after localization at `(2)`. Therefore

`R5-PRIM <=> f in A^x`.

By Lemma `RESIDUE-002`, this is equivalent to `bar f != 0` in `F_2[[T]]`.

Because `Phi_Lambda` is an isomorphism, reduction modulo `2` gives an isomorphism of free rank-one `F_2[[T]]` modules

`D_Lambda/2 -> KS_Lambda/2`.

Hence `bar f != 0` is equivalent to

`bar kappa_Lambda^{Kato} != 0` in `KS_Lambda/2`.

### Theorem `BSD-R5-PRIM-RESIDUAL-004`

On the protected selected BSD lane,

`R5-PRIM`

is equivalent to

`bar kappa_Lambda^{Kato} != 0`

in the residual inverse-limit selected Kolyvagin-system module `KS_Lambda/2`.

Equivalently, the Kato determinant coordinate has height-one `(2)` valuation zero after the algebraic determinant factor already incorporated by the determinant line.

This is an **excess-divisibility** or determinant-coordinate `mu=0` statement. It is not, without further comparison, a declaration that every conventional algebraic or analytic Iwasawa `mu` invariant appearing elsewhere in the campaign is zero.

## 6. Finite cyclotomic detection

At `m=1`,

`R_{1,n}=F_2[Gamma_n]`.

If `Gamma_n` is generated by `gamma` and `T=gamma-1`, then in characteristic two

`(1+T)^(2^n)-1 = T^(2^n)`.

Thus

`R_{1,n} ~= F_2[[T]]/(T^(2^n))`.

Finite determinant base change and the compatible `Phi_{1,n}` identify

`KS_Lambda/2 ~= lim_n KS_1(T_{1,n})`,

with the transition maps induced by

`F_2[[T]]/(T^(2^(n+1))) -> F_2[[T]]/(T^(2^n))`.

The module `KS_Lambda/2` is free rank one over the `T`-adically separated ring `F_2[[T]]`. Hence

`intersection_n T^(2^n)(KS_Lambda/2)=0`.

Therefore an element of `KS_Lambda/2` is nonzero if and only if its image is nonzero in at least one finite quotient.

### Theorem `BSD-R5-PRIM-FINITE-DETECT-005`

The following are equivalent:

1. `R5-PRIM`;
2. `bar kappa_Lambda^{Kato} != 0` in `KS_Lambda/2`;
3. there exists `n>=0` such that
   `kappa_{1,n}^{Kato} != 0` in `KS_1(T_{1,n})`.

The finite-layer condition is **nonvanishing**, not basis status.

### Proof

(1)<=> (2) is Theorem `RESIDUAL-004`. The inverse-limit description identifies the reduction of `kappa_Lambda^{Kato}` with the compatible family `(kappa_{1,n}^{Kato})_n`. T-adic separatedness gives (2)<=> (3). QED.

Since a Kolyvagin system is a compatible family of derivative components, a nonzero finite-layer system has at least one nonzero derivative component. No separate core-vertex projection theorem over the group ring is needed for this witness.

## 7. Why existing protected theorems stop here

Protected WP60T constructs the literal-`2` Kato derivative at every finite coefficient level and proves its first-component identity and Fitting containments. It explicitly does not prove that the Kato-derived system is primitive at `(2)` and does not compute the cyclotomic `mu` contribution.

Protected R5-LIFT proves integrality/membership, which is exactly the statement that the determinant coordinate `f` lies in `Lambda`. It does not prove `f notin 2Lambda`.

Protected MATHFORGE WP17B/WP17C records that Kato's all-height-one integral theorem excludes literal `p=2`, and that no screened theorem supplies uniform good-ordinary irreducible-`S_3` `mu_2=0` control.

The protected 2026 Castella–Sano source audit sharpens the same point from the Kolyvagin side: determinant basis status is governed by the normalized Kato-system divisibility index, while the paper's theorem proving that index is stated under `p>3` and its Kurihara explicit-reciprocity bridge is also an odd-prime interface.

No existing protected artifact supplies a nonzero `kappa_{1,n}^{Kato}` for some `n` uniformly over the selected class.

## 8. Exact residual boundary

Record

`MISSING_P2_RESIDUAL_CYCLOTOMIC_KATO_KOLYVAGIN_NONVANISHING`.

Equivalent formulations, under the proved reduction above, are:

- the residual inverse-limit Kato-derived Kolyvagin system is nonzero;
- the determinant coordinate has no common factor `2`;
- the excess height-one `(2)` valuation is zero;
- some finite cyclotomic mod-`2` Kato-derived system is nonzero.

A proof of bare rational nonvanishing, a nonzero characteristic-zero specialization, or a finite-layer element divisible by `2` does not discharge this boundary.

## 9. Disposition

The operation has proved a strict reduction of R5-PRIM but has not proved the residual nonvanishing theorem.

Candidate disposition:

`BLOCKED` on

`MISSING_P2_RESIDUAL_CYCLOTOMIC_KATO_KOLYVAGIN_NONVANISHING`.

This is a substantive arithmetic boundary, not a connector, CI, formatting, or source-access failure.

## 10. Claim firewall

Do not infer:

- `R5_PRIM_ESTABLISHED`;
- a finite-layer basis theorem from finite-layer nonvanishing;
- `mu_2=0` for every unrelated Selmer or p-adic L-function normalization;
- a literal-2 Castella–Sano/Kurihara theorem;
- D2d;
- `BSD-R2-A1`;
- novelty or priority;
- MATHCERT certification.
