# BSD R5-PRIM — source and boundary note

## Protected source authority

The current provider authority is

`grandchallenge/MATHFORGE@5aef30aa64730ca777329013e1e33c03f34a6e3a`.

Relevant admitted records include:

- `KATO_P2_HEIGHT_ONE_WP17B_SOURCE_AUDIT.md`;
- `P2_ORDINARY_IRREDUCIBLE_WP17C_SOURCE_AUDIT.md`;
- `BSS_P2_INVERSE_LIMIT_WP60S_SOURCE_AUDIT.md`;
- `BSS_P2_DERIVATIVE_WP60T_SOURCE_AUDIT.md`;
- `BKS_P2_EQUIVARIANT_DETERMINANT_R5_LIFT_SOURCE_AUDIT.md`;
- `KATAOKA_SANO_BASIC_EULER_P2_R5_LIFT_SOURCE_AUDIT.md`;
- `CASTELLA_SANO_R5_PRIM_DETERMINANT_DESCENT_SOURCE_AUDIT.md`.

## What changed after R5-LIFT

Earlier WP08/WP17 source work could only say that the height-one prime `(2)` / `mu` component was missing in the cyclotomic route. It did not yet possess an integral literal-`2` Kato determinant element on the exact protected line.

Protected R5-LIFT now supplies that element. The remaining question is therefore no longer an unspecified relative-`mu` mismatch between hypothetical analytic and algebraic characteristic elements. It is the concrete excess divisibility of the actual protected Kato determinant preimage:

is its coordinate divisible by `2` in the cyclotomic determinant line?

The reduction theorem in this package answers the formal part exactly: R5-PRIM holds iff the residual compatible Kato-derived Kolyvagin system is nonzero.

## Kato 2004 boundary

Protected WP17B records the source split:

- Kato's ordinary height-one control away from primes containing `p` is available at `p=2`;
- the integral all-height-one upgrade that includes `(2)` explicitly assumes `p != 2`;
- the existence of integral Euler-system generators does not determine the missing `(2)` exponent.

Thus Kato's original theorem does not prove the residual nonvanishing criterion obtained here.

## Good-ordinary irreducible `S_3` source screen

Protected WP17C specifically screened the selected residual branch

`im(E[2])=GL_2(F_2) ~= S_3`.

It found:

- known positive `mu_2` constructions based on rational `2^n`-isogenies lie on reducible residual branches;
- current `p=2` mu classification work likewise assumes reducible residual representations;
- the irreducible-residual Greenberg `mu=0` programme remains an odd-prime theorem/conjectural interface in the screened literature;
- nearby anticyclotomic full-main-conjecture results require `p>=5`;
- integral finite-layer `p=2` methods exist in other local branches but do not yield the selected uniform theorem.

No protected result proves the selected residual Kato-system nonvanishing.

## Castella–Sano 2026 architecture

The newly admitted Castella–Sano audit supplies an exact conceptual match:

- determinant integrality is strictly weaker than determinant basis status;
- determinant basis status is controlled by a normalized minimal divisibility index of Kato's Kolyvagin system;
- their structure and rigidity theorems in the relevant chain are stated for `p>3`;
- protected WP60M/R/S/T replace much of the localization/system architecture at literal `2`, but do not by themselves supply the missing normalized Kato-system primitivity value;
- Kurihara explicit reciprocity is an optional route for witnessing that value, and is itself an odd-prime interface in the source.

The present MATHSOLVE reduction avoids importing the `p>3` theorem and identifies the exact residual witness directly.

## Why characteristic-zero nonvanishing is insufficient

Kato explicit reciprocity and nonvanishing of suitable complex/cyclotomic twists can establish that the characteristic-zero Kato system is nonzero. They do not bound its common `2`-divisibility.

An element `2^a u(T)` with `a>0` can be nonzero at every characteristic-zero specialization while reducing to zero modulo `2`. R5-PRIM asks exactly that `a=0` after the protected determinant normalization.

Similarly, nonvanishing of the base first component is not the correct criterion: in analytic rank one the base localization is constrained by the vanishing of the central complex value, while higher derivative components can witness the residual inverse-limit system.

## Current external reconnaissance

A fresh current-literature screen after the R5-LIFT completion did not identify a reputable theorem proving the selected good-ordinary irreducible `p=2` statement. Current general good-ordinary main-conjecture theorems located in the screen continue to assume `p>2` or `p>=5`.

This is not a theorem-nonexistence claim. It is evidence that the exact source needed for immediate import has not been identified.

## Exact boundary

The first independent missing mathematical statement is

`MISSING_P2_RESIDUAL_CYCLOTOMIC_KATO_KOLYVAGIN_NONVANISHING`.

A proof may proceed by any route that establishes the exact residual statement, including:

1. a direct mod-`2` Kato/Kolyvagin nonvanishing theorem on the selected `S_3` branch;
2. a literal-`2` normalized Kato-system divisibility-index theorem;
3. a literal-`2` Kurihara explicit-reciprocity theorem plus a refined modular-symbol nonvanishing theorem;
4. an equivalent determinant-coordinate `mu=0` theorem on the exact protected line.

Any route must preserve the distinction between the determinant-coordinate excess factor and unrelated conventional `mu` invariants unless an exact comparison is separately proved.

## Claim discipline

The source evidence supports a `BLOCKED` disposition at this boundary. It does not support `R5_PRIM_ESTABLISHED`, D2d, `BSD-R2-A1`, novelty/priority, or MATHCERT certification.
