# Exact remaining boundary after WP14

## Frontier

`BSD-R2-A1-S3-INTEGRAL-P2-SATURATION-OR-DIRECT-LENGTH`

WP12 removes the residual-image branch ambiguity. WP13 makes every bad-prime 2-primary Tamagawa contribution explicit. WP14 shows that the admitted Chao Li mod-2 level-raising theorem does not supply the missing uniform equality: even on its applicability locus it is a rank-lowering obstruction, not an integral leading-term theorem.

The selected target, after the WP05 torsion-valuation removal, still requires the exact equality

`ord_2(L'(E,1)/(Omega_E Reg_E))`

`= len_Z2 Sha(E/Q)[2^infinity] + sum_{ell|N} ord_2(c_ell)`.

## What a closing theorem must do

A uniform theorem for the selected class must retain all of the following.

1. **Prime `2` exactly.** An odd-prime Euler/Kolyvagin/Iwasawa theorem is not a substitute.
2. **Both WP13 conductor regimes.** It must cover curves with all bad `c_ell` odd and curves with one or more even bad Tamagawa factors.
3. **Exact local defect.** In the residual-conductor-drop regime, the missing saturation/primitivity defect must reproduce the WP13 local lengths, not merely their parity or support.
4. **No extra local-at-2 assumption.** A source theorem requiring `rho_bar|G_Q2 != 1` is not uniform unless that condition is separately proved from the selected hypotheses or the complementary case is covered.
5. **No hidden minimal-Selmer premise.** The theorem may not assume `s_2(E/K)=1` unless the equivalent condition `Sha(E/K)[2]=0` is proved or the nonminimal cases are also controlled.
6. **WP00 normalization.** Complete complex `L`, period, regulator, Tamagawa, and valuation conventions must compose without an unspecified power of `2`.
7. **Noncircularity.** The desired BSD valuation may not be used as an arithmetic input.

## Mathematically admissible closing forms

Any one of the following would close the current substantive gap if proved for the full selected class with exact normalization:

- an integral `p=2` Kolyvagin-system saturation/primitivity theorem whose defect is exactly the WP13 Tamagawa length;
- a direct exact 2-primary Selmer-length theorem computing the right-hand side from the normalized complex leading term;
- an independent exact leading-term mechanism that covers both residual-conductor regimes and all local-at-2 cases.

## Current evidentiary boundary

The protected source stack presently contains:

- odd-prime exact Heegner-index/primitivity technology that does not mechanically specialize to `p=2`;
- the Chao Li mod-2 rank-lowering obstruction, whose exact applicability is now closed by WP14;
- p-adic Gross-Zagier applicability at `p=2` in the WP09 auxiliary lane, without the integral Sha/Tamagawa comparison;
- restricted CM, twist-family, modulo-squares, and individual-curve results that do not cover the selected universal class.

No currently admitted source supplies the uniform integral theorem above. External reconnaissance may search for a stronger theorem, but absence from a search is not proof that no theorem exists.

## Boundary name

`MISSING_UNIFORM_INTEGRAL_P2_ARITHMETIC_LENGTH_THEOREM`

This is a substantive mathematical/evidentiary boundary. Further campaign progress requires either a materially new theorem proved in MATHSOLVE or admission of an authoritative source theorem whose exact hypotheses and normalization cover this gap.

`BSD-R2-A1` remains unproved. MATHCERT remains pending.
