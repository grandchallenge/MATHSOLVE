# BSD R5-CW-P2-CH15 — Chapter-15 globalization compatibility replay

Operation: `BSD-R5-CW-P2-CH15` / #339.

This package consumes the protected universal half-weight cover and the
protected MATHFORGE density audit.

It proves that the square-root cover itself creates no second generic-fibre
obstruction:

- the cover is finite etale after inverting two;
- full deformation modules base-change faithfully flatly;
- Poitou–Tate exactness and torsion-annihilator arguments survive;
- density, if available on the base, pulls back to the cover;
- the protected selected `S_3` residual representation remains irreducible
  over the cyclotomic `Z_2`-extension;
- the finite sign factor splits after inverting two.

The operation is therefore `BLOCKED` only on

`MISSING_P2_FULL_DEFORMATION_LOCALLY_IRREDUCIBLE_CLASSICAL_DENSITY_ON_SELECTED_S3_LANE`.

The later denominator-removal obligation is independent and remains open.
