# BSD R5-LIFT — equivariant cyclotomic determinant/Stark/Kolyvagin replay at literal `p=2`

## Status

Candidate substantive theorem package for `grandchallenge/MATHSOLVE#262`.

No claim in this package is protected until exact-head non-authoring/read-only Adversary and Referee passes, required CI, protected merge/readback, and the operation completion receipt all succeed.

## Exact entering anchors

- MATHSOLVE protected BSD base: `270e2c2f9dde7bd817a251c9e3fb250fff36a81b`.
- MATHFORGE protected provider: `de2c83c2c440ac357b183b9e1018e9f69c58e1d9`.
- INTELLECT constitutional authority: `f042220f3bed7cb7b5069256e8f6305c850c0628`.
- Active staffing directive: `GI-STEWARD-0003`.
- Issue: `grandchallenge/MATHSOLVE#262`.
- Parent tracker: `grandchallenge/MATHSOLVE#215`.
- Programme owner: `grandchallenge/MATHSOLVE#164`.

## Entering mathematical boundary

`MISSING_P2_EQUIVARIANT_CYCLOTOMIC_DETERMINANT_STARK_KOLYVAGIN_REPLAY`.

This is the exact group-ring sub-boundary of

`MISSING_P2_KATO_ZETA_FITTING_DIVISIBILITY_AT_HEIGHT_ONE_2` (`R5-LIFT`).

## Candidate theorem

For every cyclotomic layer `Q_n/Q` of the cyclotomic `Z_2`-extension and every `m>=1`, put

`R_{m,n}=Z/2^m[Gamma_n]`, `Gamma_n=Gal(Q_n/Q)`.

The package proves, on the exact selected `BSD-R2-A1` lane:

1. `R_{m,n}` is a local zero-dimensional Gorenstein ring with residue field `F_2`;
2. the fixed protected multiplicative-inertia element `tau` gives a free rank-one quotient of the equivariant coefficient representation;
3. the source's false formal H3.2(iii) is replaced, on the actual selected modified Selmer classes, by a cyclotomic-layer version of the WP60M defect-exclusion theorem;
4. the WP60R characteristic-two localization/core-graph argument lifts to `R_{m,n}` through the one-dimensional socle and Nakayama reduction;
5. sufficiently many large products and core vertices therefore exist, giving the finite-free presentations required by the determinant-to-Stark map;
6. the rank-one Stark-to-Kolyvagin regulator is an isomorphism over every `R_{m,n}`;
7. the selected literal-2 Euler-system derivative has first component equal to the finite-layer Kato class;
8. the constructions are compatible in `m,n` and yield cyclotomic determinant membership;
9. the real-place correction is retained but has unit determinant ideal; the genuine ordinary strict/Kummer correction is retained and is supported at `(2,gamma-1)`, hence vanishes after localization at the height-one prime `(2)`;
10. protected WP60A-A1 then identifies this determinant membership with the one-sided height-one `(2)` Fitting divisibility `R5-LIFT`.

The package does **not** establish determinant primitivity.

## Candidate disposition

`CLOSED`, subject to governed admission.

If protected, retire both

- `MISSING_P2_EQUIVARIANT_CYCLOTOMIC_DETERMINANT_STARK_KOLYVAGIN_REPLAY`;
- `MISSING_P2_KATO_ZETA_FITTING_DIVISIBILITY_AT_HEIGHT_ONE_2`.

The next independent R5 boundary is

`MISSING_P2_DETERMINANTAL_ZETA_PRIMITIVITY_AT_HEIGHT_ONE_2`.

## Claim firewall

This package does not assert:

- formal higher-level BSS Hypothesis 3.2(iii);
- full BSS Hypothesis 4.7 or clause (iii);
- infinite BSS H3;
- determinant primitivity or `R5-PRIM`;
- D2d;
- `BSD-R2-A1`;
- novelty or priority;
- MATHCERT certification.
