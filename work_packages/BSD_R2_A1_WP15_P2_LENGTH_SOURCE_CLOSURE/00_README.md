# BSD-R2-A1-WP15 — `p=2` length-source closure

## Metadata

- Campaign: `BSD-001`.
- Work package: `BSD-R2-A1-WP15-P2-LENGTH-SOURCE-CLOSURE`.
- Native owner: `grandchallenge/MATHSOLVE#162`.
- Protected Solve baseline: `b088e2cc4bd8910f06cf0ecb935da21f556630e3`.
- Protected Forge source baseline: `118ae1b5c2fc2630f53000921b742c610c50db16`.
- Parent frontier: `BSD-R2-A1-S3-INTEGRAL-P2-SATURATION-OR-DIRECT-LENGTH`.
- Selected claim: `BSD-R2-A1 = SELECTED_RESEARCH_TARGET_UNPROVED`.

## Result

WP15 closes the named square-class and screened-literature escape routes without promoting the selected BSD identity.

It proves in-package that:

1. `E(Q)` has no 2-primary rational torsion;
2. the protected analytic-rank-one interface makes `Sha(E/Q)` finite;
3. the admitted Cassels–Tate pairing then forces `#Sha(E/Q)` to be a square, hence
   `len_Z2 Sha(E/Q)[2^infinity]` is even;
4. therefore a rank-one BSD statement only modulo rational squares can at the prime `2` determine at most

   `ord_2(L'(E,1)/(Omega_E Reg_E)) = sum_{ell|N} ord_2(c_ell) (mod 2)`;

5. this parity statement cannot determine the exact even integer

   `len_Z2 Sha(E/Q)[2^infinity]`;

6. the protected Forge screen confirms that the four named literature routes do not supply the missing unsquared uniform `p=2` integer control: Barrios–Mok transports square classes, Jetchev is odd-prime, Kriz–Li is a restricted positive BSD(2) twist lane, and Yan–Zhu assumes `p>2`.

## Consequence

The mathematical dependency is sharpened from a generic `p=2` source gap to an exact unsquared-length gap. A closing argument needs new integer-valued `2`-primary control; parity or square-class transport is structurally insufficient.

## Claim boundary

WP15 does not prove `BSD-R2-A1`, a `p=2` Kolyvagin-system primitivity theorem, a sharp `p=2` Kolyvagin bound, an exact Sha/index formula, novelty/priority, or MATHCERT certification. The source screen is bounded and is not a claim of literature exhaustiveness.