# Exact source-composition matrix after WP14

## Protected provider authority

WP15 uses only the protected MATHFORGE state

`118ae1b5c2fc2630f53000921b742c610c50db16`.

The relevant admitted records are:

- `sources/BSD-001/P2_LENGTH_ESCAPE_ROUTE_SOURCE_AUDIT.md`;
- `sources/BSD-001/CASSELS_TATE_PAIRING_SOURCE_AUDIT.md`.

The first is a bounded four-route screen. It is not a literature-exhaustiveness claim.

## Route A — Barrios–Mok modulo-square transport

The admitted interface records Theorem 1.1 and Corollary 1.5 of arXiv:`2510.00926v1`.

What it supplies: under its low-rank quadratic-twist hypotheses, validity of BSD modulo `(Q^x)^2` is transported between the two twists.

What it does not supply:

- an absolute modulo-square BSD theorem for every selected curve;
- an unsquared valuation;
- an exact `2`-primary Sha length.

The in-package theorem `BSD-A1-SQ001` proves that even an absolute modulo-square theorem would reduce the selected `p=2` identity only to parity.

**Disposition:** `PARITY_ONLY_NOT_EXACT_LENGTH`.

## Route B — Jetchev Tamagawa-aware Kolyvagin bound

The admitted source explicitly starts with an **odd prime** `p` before Hypothesis `(*)` and proves its Tamagawa-aware Kolyvagin bound in that framework.

Protected WP12 discharges residual surjectivity for the selected class and the selected conductor is odd, but neither fact changes the source prime range.

**Disposition:** `PRIME_RANGE_EXCLUDES_P2`.

No sharp one-sided `p=2` bound may be manufactured by substituting `p=2` into this theorem.

## Route C — Kriz–Li BSD(2) twist lane

The admitted interface is a genuine positive theorem at `p=2`.

Its mechanism is nevertheless restricted:

- the Heegner/congruence package of the source is required;
- the relevant arithmetic lane has odd local Tamagawa control / mod-2 Heegner indivisibility;
- Q-side BSD(2) propagation requires seed BSD(2) input;
- the theorem propagates through a constructed twist family rather than covering every curve satisfying `BSD-R2-A1`.

Protected WP13 shows that the selected class includes a second regime with even Tamagawa factors. That regime is not covered by the source mechanism recorded in the audit.

**Disposition:** `POSITIVE_P2_RESTRICTED_NOT_UNIFORM`.

## Route D — Yan–Zhu ordinary main-conjecture lane

The admitted abstract-level interface fixes a good ordinary prime

`p > 2`.

Its low-rank `p`-part BSD applications therefore cannot be instantiated at `p=2`.

**Disposition:** `PRIME_RANGE_EXCLUDES_P2`.

## Joint theorem

None of the four **screened interfaces** supplies any of the following for the full selected class:

1. a uniform exact unsquared `p=2` length theorem;
2. a uniform `p=2` saturation/primitivity theorem with local defect exactly equal to the WP13 Tamagawa length;
3. a sharp one-sided `p=2` bound that can be combined with `BSD-A1-SQ001` to force the exact integer;
4. a direct exact `Q`-side arithmetic-length formula in the WP00 normalization.

The word *screened* is essential. This conclusion is about these admitted interfaces only. It is not evidence that no suitable theorem exists elsewhere.

## What remains mathematically useful

The source screen still changes the frontier:

- square-class transport is now known to be too coarse on structural grounds, not merely unavailable;
- the attractive Tamagawa-aware bound is ruled out specifically by its odd-prime theorem statement;
- Kriz–Li demonstrates that exact BSD(2) technology exists on a restricted locus, so `p=2` itself is not a universal impossibility; the missing feature is **uniform unsquared control across the selected class**, including WP13's even-Tamagawa regime;
- contemporary non-CM ordinary main-conjecture results screened here still begin above `2`.

This isolates the remaining mathematical obligation without inflating it into a no-go theorem.