# WP56A theorem — exact classical height/regulator descent

## 1. Setup

Let `E/Q` lie in the protected selected `BSD-R2-A1` class and let `K/Q` be the protected WP09 auxiliary imaginary quadratic field. Let

`P_K(f) in E(K)`

be the conductor-one trivial-character Heegner trace appearing in the protected Cai–Shu–Tian source interface.

Choose a primitive generator

`P in E(Q)/E(Q)_tors`.

Protected WP38 proves that base change induces an isomorphism

`E(Q)/E(Q)_tors ->~ E(K)/E(K)_tors`.

## 2. Heegner-index decomposition

### Lemma `BSD-A1-WP56-MW-INDEX-001`

There are an integer `m_K(f)` and a torsion point `T in E(K)_tors` such that

`P_K(f)=m_K(f)P+T`.

The absolute value `|m_K(f)|` is independent of the sign chosen for the primitive generator. Moreover `m_K(f) != 0`.

### Proof

The protected WP38 isomorphism identifies the free quotient `E(K)/E(K)_tors` with the infinite cyclic group generated by the image of `P`. Hence the image of `P_K(f)` is a unique integer multiple `m_K(f)[P]`, yielding the displayed equality after adding torsion. Replacing `P` by `-P` changes the sign of `m_K(f)` only.

Protected WP09 gives

`ord_{s=1} L(E/K,s)=1`,

so `L'(E/K,1) != 0`. Protected MATHFORGE WP55 gives the exact CST formula expressing this derivative as a nonzero explicit scalar times `hhat_K(P_K(f))`. Hence `hhat_K(P_K(f)) != 0`, so `P_K(f)` is nontorsion and `m_K(f) != 0`. QED.

## 3. Rank-one WP00 regulator

### Lemma `BSD-A1-WP56-REG-001`

With the WP00 Néron–Tate pairing convention,

`Reg_E = hhat_Q(P)`.

### Proof

WP00 defines `Reg_E` as the determinant of the Néron–Tate height-pairing matrix on a `Z`-basis of the free Mordell–Weil group. In rank one the matrix is the `1 x 1` matrix `(<P,P>_NT)`. For the canonical quadratic height, polarization gives

`<P,P>_NT
 = (1/2)(hhat_Q(2P)-2hhat_Q(P))
 = (1/2)(4hhat_Q(P)-2hhat_Q(P))
 = hhat_Q(P)`.

Thus its determinant is `hhat_Q(P)`. QED.

## 4. Exact K-versus-Q height conversion

Protected MATHFORGE WP56 admits, for the exact CST/YZZ conventions on the selected quadratic lane,

`hhat_K(Q)=2 hhat_Q(Q)`

for every point in the common domain.

Canonical height is quadratic and vanishes on torsion. Therefore

`hhat_Q(P_K(f))
 = hhat_Q(m_K(f)P+T)
 = m_K(f)^2 hhat_Q(P)`.

Multiplying by the exact base-field factor gives

`hhat_K(P_K(f))
 = 2 m_K(f)^2 hhat_Q(P)`.

Using Lemma `REG-001` yields the main theorem.

### Theorem `BSD-A1-WP56-HEIGHT-REG-001`

Exactly,

`hhat_K(P_K(f))/Reg_E = 2 m_K(f)^2`.

There is no hidden base-change index, unspecified unit, or omitted power of `2`.

Since the right-hand side is a positive rational integer,

`ord_2(hhat_K(P_K(f))/Reg_E)
 = 1 + 2 ord_2(m_K(f))`.

The valuation here is legitimate because the ratio has first been identified exactly with the ordinary integer `2m_K(f)^2`; it is not a valuation of an arbitrary real period ratio.

## 5. Substitution into WP55A

Protected WP55A proves

`L'(E,1)/(Omega_E Reg_E)
 = [2 A_E/(C_f^2 u_K^2 sqrt(|D_K|) Omega_E L(E^D,1))]
   * [hhat_K(P_K(f))/Reg_E]`.

Substitute Theorem `HEIGHT-REG-001`:

### Corollary `BSD-A1-WP56-WP00-RESIDUAL-001`

`L'(E,1)/(Omega_E Reg_E)
 = 4 m_K(f)^2 A_E
   / (C_f^2 u_K^2 sqrt(|D_K|) Omega_E L(E^D,1))`.

This is an exact real identity.

It isolates the remaining classical/WP00 normalization into:

1. the genuine integer Heegner index `m_K(f)`;
2. the scalar
   `A_E/(C_f^2 u_K^2 sqrt(|D_K|) Omega_E L(E^D,1))`.

No `2`-adic valuation is assigned to item 2 until an exact algebraicity/rational-line comparison identifies the complete product with a rational/algebraic quantity in a fixed `2`-adic embedding.

## 6. Exact remaining boundary

WP56A resolves the former height-convention subdefect. It does not determine the Heegner index.

The current D2d boundary is refined to

`MISSING_EXACT_HEEGNER_INDEX_AND_AREA_PERIOD_TWIST_SCALAR_COMPARISON`.

The two subobligations are independent enough to pursue separately:

- `H1`: determine the exact selected-branch `2`-primary Heegner index contribution `ord_2(m_K(f))`, without importing an odd-prime primitivity theorem;
- `H2`: identify the remaining area/period/modular-differential/twist-central-value scalar on an exact algebraic line and compute its `2`-adic contribution.

## 7. Firewall

This theorem does not prove:

- `m_K(f)=1` or `ord_2(m_K(f))=0`;
- a literal-`p=2` Heegner primitivity theorem;
- fixed-`2` p-adic-height nondegeneracy;
- a height-one `(2)` analytic determinant generator;
- rank-zero BSD for `E^D`;
- cancellation with WP54A class-number/unit or bad-prime factors;
- the final WP06 normalization descent;
- `BSD-R2-A1`;
- MATHCERT certification.
