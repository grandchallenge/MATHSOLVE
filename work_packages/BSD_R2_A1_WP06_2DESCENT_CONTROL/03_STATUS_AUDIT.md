# Known terrain and status audit

## Protected campaign input

WP05 established that the selected rank-one target remains open precisely at the integral 2-primary control/reciprocity interface. It prohibited silent transfer of odd-prime plus/minus decompositions to `p=2`.

## External concordance only

A current literature screen found two directly relevant classical structures.

1. Kenneth Kramer, *Arithmetic of elliptic curves upon quadratic extension*, Trans. AMS 264 (1981), studies the 2-Selmer group under quadratic extension and expresses its discrepancy through local norm indices.
2. Adam Morgan and Ross Paterson, *On 2-Selmer groups of twists after quadratic extension*, JLMS 105 (2022), reconstruct Kramer's mod-2 restriction/corestriction sequence in Selmer-structure language. Their Section 4 explicitly does not assume rational 2-torsion until later applications.

These sources are used here as concordance and falsification checks. They are not silently promoted into the retrospective BSD MATHFORGE provider manifest.

## New package deduction

WP06 derives the finite `2^n` control statement directly from:

- the subgroup structure of `GL_2(F_2)`;
- inflation-restriction;
- the Kummer exact sequence;
- exact elementary algebra for a module with an involution.

The resulting theorem is valid for every `n >= 1`. It does not extrapolate the characteristic-2 augmentation sequence used in the mod-2 literature.

## Scope of the advance

The descent obstruction is therefore closed at the level required by WP05: every quadratic base-change defect is represented by a named finite exponent-2 group. What remains is not a descent ambiguity. It is the substantive analytic-to-arithmetic input: an integral p=2 Iwasawa/Euler-system length and explicit reciprocity/height comparison.