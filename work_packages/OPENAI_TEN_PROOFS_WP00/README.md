# OPENAI-TEN-PROOFS-WP00 — Blocked theorem-concordance route

## Status

`blocked_pre_route_candidate`. No MATHCERT handoff exists or is authorized.

## Future Solve obligation

After the Forge source lock and Programme governance record become protected-branch authority, MATHSOLVE must construct a result-family ledger with the exact mapping:

`advertised result → source theorem and revision → Lean declaration → Comparator target`.

The ledger must treat the twelve advertised result families independently. It must not infer source equivalence or mathematical truth from repository-wide compilation.

## Semantic priorities

The first adversarial pass must target custom encodings of NP-hardness, promise problems, soficity, finite presentation, tracial group-factor isomorphism, asymptotic rate and sharpness, and counterexample nonvacuity. For each target it must audit carrier types, quantifier order, malformed inputs, imported assumptions, witness existence, and equivalence to the cited source statement.

## Current blockers

- MATHFORGE PR #36 is unmerged.
- MATH-PROGRAMME PR #199 is unmerged.
- The upstream default target `ConnesRigidity2` is undeclared.
- The configured Comparator root `ComparatorChallenges.C_PermanentSuperquadraticStandalone` is missing.
- Trusted exact-head build, Comparator replay, and theorem-level axiom reports are incomplete.
- Upstream `formalization.yaml` records no sources.

## Claim boundary

This package is a fail-closed route guard. It is not a campaign manifest, Cert handoff, proof, certification output, source-equivalence finding, or novelty record.
