# Next analytic target

## `BSD-R2-A1-2PRIMARY-HEEGNER-INDEX`

WP09 closes auxiliary-field existence and corrected Disegni applicability. The next preferred lane is a direct arithmetic composition that can potentially bypass the independent WP08 cyclotomic-mu defect.

### Target

Determine whether the selected hypotheses admit an exact theorem package which, for a suitable auxiliary `K` and its Heegner class `P_K`, combines:

1. a fully normalized complex Gross-Zagier identity for the same Heegner class;
2. the factorization
   `L'(E/K,1) = L'(E,1)L(E^D,1)`;
3. an exact integral 2-primary Kolyvagin/Heegner-index relation;
4. descent from the auxiliary field back to `Q` with every exponent-2 kernel/cokernel already exposed by WP06;
5. the WP00 Tamagawa, real Neron period, Neron-Tate regulator, isogeny, Manin, and torsion conventions.

The desired arithmetic output is an equality sufficient to determine

`len_Z2 Sha(E/Q)[2^infinity] + sum_{ell|N} ord_2(c_ell)`

from

`ord_2(L'(E,1)/(Omega_E Reg_E))`.

### Acceptance rule

A theorem stated only:

- for odd `p`;
- after inverting `2`;
- up to a 2-adic unit;
- up to an unspecified power of `2`;
- under residual 2-distinguishedness excluded by WP07;
- or for a strict special family not containing the selected class

is not composable.

### Alternative Iwasawa lane

If the direct arithmetic lane fails, the remaining alternative is to close both:

- the WP08 height-one `(2)` / relative-mu exponent; and
- the exact comparison from the WP09 p-adic Gross-Zagier identity to the WP00 complex leading term.

That alternative remains admissible but currently has strictly more independent theorem debt.

### Provider requirement

Any new literature theorem used as a proof premise must first be admitted or explicitly waived through MATHFORGE. External reconnaissance may nominate candidates but may not promote a claim.