# WP60E theorem — bounded finite-level R5 source-interface barrier

## 1. Protected setup

Let `E/Q` lie in the protected selected `BSD-R2-A1` class. Protected WP60A-A1 has already proved that at the cyclotomic height-one prime `(2)`, determinant membership for the fixed Kato class is exactly the missing one-sided Fitting divisibility.

The live R5 obligations are therefore:

`R5-LIFT = MISSING_P2_KATO_ZETA_FITTING_DIVISIBILITY_AT_HEIGHT_ONE_2`,

`R5-PRIM = MISSING_P2_DETERMINANTAL_ZETA_PRIMITIVITY_AT_HEIGHT_ONE_2`.

Protected MATHFORGE WP60E at

`grandchallenge/MATHFORGE@8d49d253fd10708f09b8cafa262de276bed88f23`

audits the finite-level Kato-derivative / determinantal-zeta / Mazur–Tate / Fitting frameworks of Burns–Kurihara–Sano and Chan-Ho Kim.

## 2. Exact admitted interfaces

The provider audit records:

1. Burns–Kurihara–Sano, *On derivatives of Kato's Euler system for elliptic curves*:
   - the paper fixes an odd prime `p` globally;
   - its concrete finite-level Fitting containment, Theorem 1.3 / Proposition 3.3, requires `p>3`.

2. Burns–Kurihara–Sano, *On derivatives of Kato's Euler system and the Mazur-Tate Conjecture*:
   - the determinantal-zeta/Mazur–Tate argument is developed under Hypothesis 2.2;
   - Hypothesis 2.2 requires `p` not to divide an integer containing the factor `6`;
   - literal `p=2` is therefore excluded.

3. Chan-Ho Kim, *Refined applications of Kato's Euler systems for modular forms*:
   - the paper fixes odd `p`;
   - its finite-layer Mazur–Tate/Fitting result assumes odd `p`;
   - its main Kolyvagin-system/main-conjecture structural theorem assumes `p>=5`.

No protected extension removing these hypotheses is admitted.

## 3. Barrier theorem

### Proposition `BSD-A1-WP60E-FINITE-R5-BARRIER-001`

From the finite-level derivative/determinantal/Fitting source interfaces currently protected by MATHFORGE WP60E, there is no valid inference that proves either `R5-LIFT` or `R5-PRIM` for the selected literal-`p=2` class.

In particular, the currently admitted finite-level interfaces do not furnish a direct primitive-Kummer/Fitting bypass of the cyclotomic height-one-`(2)` exponent.

### Proof

Every protected theorem in the bounded WP60E screen that supplies the relevant finite-level Fitting or determinantal conclusion has a standing prime hypothesis excluding `p=2`: respectively `p>3`, a divisibility hypothesis containing `p∤6`, or odd `p`/`p>=5`.

The selected campaign prime is exactly `p=2`. A theorem whose standing hypotheses exclude `p=2` cannot be specialized to this lane. Formal similarity of the target Fitting ideal, determinantal zeta element, or Mazur–Tate element does not remove a source hypothesis.

Therefore none of the protected WP60E interfaces proves the literal-`p=2` R5 obligations. QED.

## 4. Exact subordinate boundary

Record

`MISSING_LITERAL_P2_FINITE_LEVEL_KATO_DERIVATIVE_FITTING_THEOREM`.

This boundary is subordinate to `R5-LIFT` and `R5-PRIM`. It names the missing source/proof interface for the specifically screened finite-level architecture; it does not replace the parent R5 theorem debts.

## 5. Reopening contract

The finite-level derivative route reopens only if at least one of the following is supplied:

### E1 — literal-2 extension

A theorem with the required finite-level Fitting/determinantal conclusion whose actual standing hypotheses permit `p=2`.

### E2 — hypothesis-removal proof

An independent proof removing the odd-prime restriction from one of the screened finite-level theorems while preserving its integral Fitting statement, local conditions, and normalization.

### E3 — direct primitive finite theorem

A new literal-`p=2` theorem directly relating the relevant Kato/analytic class to the primitive finite Kummer Fitting ideal, bypassing cyclotomic `mu` without silently changing local conditions.

A result only after inverting `2`, only modulo a `2`-power error, or only at height-one primes away from `(2)` does not satisfy E1–E3.

## 6. Execution consequence

Do not spend another bounded operation re-screening the same Burns–Kurihara–Sano or Chan-Ho Kim results for literal `p=2` unless a new version or an actual hypothesis-removal argument has appeared.

The productive R5 directions are now proof construction:

1. isolate the exact role of the odd-prime restriction in the equivariant Euler-system/Fitting argument and determine whether the selected surjective `E[2]` hypotheses permit a replacement lemma;
2. construct a literal-`2` finite primitive Kummer/Fitting theorem directly; or
3. construct the missing height-one-`(2)` cyclotomic Fitting divisibility by another integral reciprocity mechanism.

## 7. Relation to the other WP60 routes

WP60E does not alter protected WP60D:

- R1 remains live;
- R2 remains live;
- R3 remains retired on the selected lane;
- R4 remains reduced to the fixed base analytic leading-term valuation;
- R5 remains live but the screened finite-level literature route is now bounded by the exact barrier above;
- D2a remains live.

## 8. Claim firewall

WP60E does not prove or imply:

- the missing height-one-`(2)` Fitting divisibility;
- determinant primitivity at `(2)`;
- nonexistence of a future literal-`p=2` finite-level theorem;
- vanishing of any relative `mu` invariant;
- `BSD-R2-A1`;
- MATHCERT certification, novelty, or priority.
