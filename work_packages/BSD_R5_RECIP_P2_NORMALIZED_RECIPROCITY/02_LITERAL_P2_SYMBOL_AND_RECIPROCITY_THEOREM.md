# BSD R5-RECIP — literal-2 modular-symbol normalization and normalized reciprocity

## 1. Protected inputs

Use the local theorem in this package; protected WP58A; protected WP60T; and MATHFORGE provider `cd814844128167d0e4cdb48f69f9d01c6c0be883`, which admits the Kim/KKS dependency decomposition and the literal-2 Kato/Mazur–Tate amendment.

Let `E_0` be the `X_0(N)`-optimal curve and `f_0:X_0(N)->E_0` its optimal parametrization. Protected semistability gives squarefree `N`, and protected WP58 gives Manin constant `+-1`. Let `psi:E_0->E` be the protected minimum-degree isogeny; WP58A proves `deg(psi)` and the differential scalar `a_psi` are odd.

## 2. Selected-lane 2-saturation of the relative modular-symbol lattice

Let `L_0` be the Néron period lattice of `E_0` and `Lhat_f` the lattice generated by integrals of the newform along relative homology paths between cusps.

### Lemma `BSD-R5-RECIP-SYMBOL-IRRED-001`

`E_0[2]` is irreducible and `E_0(Q)[2]=0`.

### Proof

The odd-degree isogeny `psi` induces an isomorphism `E_0[2] ~= E[2]`. Protected WP12 gives surjective, hence irreducible, `E[2]`. QED.

### Theorem `BSD-R5-RECIP-SYMBOL-SATURATION-002`

`Lhat_f tensor Z_2 = L_0 tensor Z_2`.

### Proof

For squarefree conductor all cusps of `X_0(N)` are rational. The quotient `Lhat_f/L_0` is generated by the images of those cusps under the optimal parametrization, hence embeds into the finite group `E_0(Q)_tors`. By `IRRED-001`, that finite group has no element of order two, so the quotient has odd order. Tensoring with `Z_2` gives equality. QED.

Transport through `psi` and `psi^*omega_E=a_psi omega_0` changes the selected relative period lattice only by odd indices/units. Therefore the selected parametrization `f=psi o f_0` has the same 2-saturation property.

## 3. Exact real-period factor

Let `Omega_E^+` denote Kim's real Néron period, the integral of the minimal invariant differential over the full real locus. Let `Omega_prim` be the least positive real element of the Néron lattice.

### Lemma `BSD-R5-RECIP-SYMBOL-REALPERIOD-003`

For every period `lambda` in the selected Néron lattice,

`2 Re(lambda)/Omega_E^+ in Z`.

### Proof

A conjugation-stable elliptic period lattice has one of two real forms.

If `E(R)` has two components, the lattice is rectangular. Then `Re(lambda)` is an integral multiple of `Omega_prim`, while `Omega_E^+=2 Omega_prim`; the ratio is half-integral.

If `E(R)` is connected, the lattice is the half-shifted form. Then `Re(lambda)` is a half-integral multiple of `Omega_prim`, while `Omega_E^+=Omega_prim`; again the ratio is half-integral. QED.

Combining `SATURATION-002`, odd transport to `E`, and `REALPERIOD-003` gives, for every selected plus modular symbol,

`2 [a/n]^+ in Z_2`.

This is deliberately weaker than raw coefficientwise 2-integrality and is compatible with the Wuthrich p=2 warning admitted by MATHFORGE.

## 4. The integral normalized Kurihara object

Protected local theorem `LOCAL-TEXP-003` gives `t_2>=1`. Define

`Delta_n^(2) := 2^t_2 * delta_tilde_n`.

Since every coefficient `[a/n]^+` is in `(1/2)Z_2`, every `Delta_n^(2)` is integral and has a canonical reduction in `Z_2/I_n`.

### Theorem `BSD-R5-RECIP-SYMBOL-INTEGRAL-004`

`Delta_n^(2) in Z_2/I_n` is well-defined at every protected finite derivative level.

Moreover, if `t_2>=2`, then

`Delta_n^(2)=0 mod 2`

for every `n`.

The latter statement concerns this analytic local-regulator witness only; it does not imply that the global residual Kato/Kolyvagin class is zero.

## 5. Literal-2 scaled KKS derivative replay

The KKS derivative computation uses the integral group-ring identity

`(sigma_l-1)D_l = (l-1)-Tr_l`

and Hecke relations with integral coefficients. It is linear in the modular-symbol group-ring element. The source decomposition already separates this combinatorial step from the later non-anomalous scalar.

Multiplying the entire computation by `2^t_2` makes every modular-symbol coefficient integral by §4. Therefore the same algebra may be performed over `Z_2/I_n`, with no inversion of `2`.

### Theorem `BSD-R5-RECIP-SYMBOL-KKS-005`

For every protected derivative index `n`, the selected literal-2 scaled derivative identity holds:

`2^t_2 * D_n(Theta_n^+) = Delta_n^(2) mod I_n`,

in the same augmentation/derivative quotient used by the protected KKS interface. No `(NA)` unit hypothesis is used in this combinatorial statement.

## 6. Combination with Kato explicit reciprocity

The protected provider admits Kato's cyclotomic explicit reciprocity mechanism at literal `2`. For good reduction its local Euler scalar after the derivative/augmentation step is

`E_2 = 1-a_2/2+1/2 = q_2/2`.

Thus the integral Kato derivative satisfies before local-lattice normalization

`exp^*_{omega_E}(loc^s_2(kappa_n^Kato))
 = unit * (q_2/2) * delta_tilde_n`.

The local theorem gives the exact generator of the dual-exponential lattice

`lambda_2 = q_2/2^(t_2+1)`.

Hence exactly

`(q_2/2) * delta_tilde_n
 = lambda_2 * Delta_n^(2)`.

The selected optimal-to-selected differential scalar and all remaining parametrization transports are 2-adic units by WP58A. The torsion-coefficient map is canonical on the integral-lift Kato derivative. Therefore, after fixing any lattice isomorphism `xi_n:lambda_2 Z_2/I_n lambda_2 Z_2 ~= Z_2/I_n`, all basis choices differ only by a unit.

### Theorem `BSD-R5-RECIP-NORMALIZED-RECIPROCITY-006`

For every protected finite Kato derivative level,

`xi_n(exp^*_{omega_E,I_n}(loc^s_2(kappa_n^Kato)))
 = u_n * Delta_n^(2) in Z_2/I_n`,

where `u_n in (Z_2/I_n)^x`.

This establishes the selected literal-2 normalized anomalous-ordinary Kato/Kurihara reciprocity law.

## 7. Disposition and next theorem

Retire

`MISSING_P2_NORMALIZED_ANOMALOUS_ORDINARY_KATO_KURIHARA_RECIPROCITY`

with candidate disposition `CLOSED`.

The first independent remaining theorem for the R5-RES route is

`MISSING_P2_NORMALIZED_FINITE_KURIHARA_NONVANISHING_WITNESS`:

produce a protected finite derivative index `n` for which `Delta_n^(2)` is nonzero modulo `2`, or prove residual nonvanishing by a different protected route.

If `t_2>=2`, the first alternative is impossible by `SYMBOL-INTEGRAL-004`, so a different residual witness is required on that sublane.

## 8. Firewall

This theorem does not establish residual Kato/Kolyvagin nonvanishing, R5-RES, R5-PRIM, D2d, BSD-R2-A1, novelty/priority, public certification, or MATHCERT certification.