# BSD R5-RES — finite-layer residual Kato/Kolyvagin detection

## Status

Substantive successor operation for `grandchallenge/MATHSOLVE#273`.

Entering protected MATHSOLVE authority:

`grandchallenge/MATHSOLVE@36202d97e2cd96956c8a4aafa43941f6219776c5`.

Protected source authority:

`grandchallenge/MATHFORGE@7da3f07248b5d8cf3d0bd1e57e9d8b7f5b78d063`,

including:

- `sources/BSD-001/R5_RES_P2_RESIDUAL_KATO_KOLYVAGIN_SOURCE_AUDIT.md`;
- `sources/BSD-001/R5_RES_KIM_REFINED_RECIPROCITY_AMENDMENT.md`.

## Entering theorem frontier

`MISSING_P2_RESIDUAL_CYCLOTOMIC_KATO_KOLYVAGIN_NONVANISHING` (`R5-RES`).

Protected `BSD-R5-PRIM` proves the exact height-one reduction

`R5-PRIM <=> bar(kappa^Kato_Lambda) != 0 in KS_Lambda/2`

and proves finite detection: it is enough to exhibit one legitimate finite cyclotomic mod-2 image of the protected inverse-limit Kato-derived Kolyvagin system that is nonzero. Finite-layer basis status is not required.

## Source-first result of this operation

The strongest admitted finite witness architecture is the refined Kato/Kurihara explicit-reciprocity route. In its protected source normalization, Chan-Ho Kim's Theorem 3.11 (`p>=5`) has the shape

`xi o exp* o loc(kappa_n^Kato) = unit * p^t * delta_tilde_n`,

with

`p^t = #E(Q_p)[p^infinity]`.

Protected WP60M/R/S/T already supplies the selected literal-2 global Kolyvagin-system, localization, derivative and inverse-limit infrastructure. The first missing interface is therefore local/integral rather than global:

`MISSING_P2_NORMALIZED_ANOMALOUS_ORDINARY_KATO_KURIHARA_RECIPROCITY`.

A literal-2 replay must construct the torsion-coefficient local regulator/dual exponential, compute its integral image, retain the exact local `2^t` factor and any period/Manin factor, and prove compatibility with the protected finite detector.

## Candidate disposition

`BLOCKED`.

This package does not establish the missing reciprocity theorem, a normalized nonzero Kurihara witness, or R5-RES. It records that no currently admitted source allows the raw odd-prime reciprocity formula to be specialized to `p=2`, and it makes the exact reopening obligation durable.

## Claim firewall

This operation does not establish:

- `R5_RES_ESTABLISHED`;
- `R5_PRIM_ESTABLISHED`;
- D2d;
- `BSD-R2-A1`;
- novelty or priority;
- public certification;
- MATHCERT certification.

R5-LIFT remains protected; R5-PRIM remains protected as a reduction with BLOCKED disposition.