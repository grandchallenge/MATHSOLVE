# BSD R5-RES — finite-layer residual detection and reciprocity boundary theorem

## Theorem identifier

`BSD-R5-RES-BOUNDARY-001`.

## Protected hypotheses and interfaces

Work on the selected BSD-001 good-ordinary literal-`p=2` lane with the protected residual/surjective `S_3` representation assumptions already used by WP60M/R/S/T.

Use without reopening:

1. protected `BSD-R5-PRIM`, which identifies the height-one `(2)` primitivity problem with residual nonvanishing of the inverse-limit selected Kato-derived Kolyvagin system and proves finite detection;
2. protected WP60M/R/S/T, which provides the selected literal-2 global restriction repair, localization/core-graph layer, inverse-limit compatibility, and Kato derivative/first-component compatibility;
3. protected WP23/WP46A local-control records, which retain the genuine good-ordinary local correction at `2` and its support, but do not assert an explicit-reciprocity identity;
4. protected MATHFORGE provider `7da3f07248b5d8cf3d0bd1e57e9d8b7f5b78d063`.

## Part A — finite detection is already sufficient

Let

`bar(kappa_Lambda^Kato)`

be the residual inverse-limit Kato-derived Kolyvagin system from protected R5-PRIM.

Protected R5-PRIM proves that

`bar(kappa_Lambda^Kato) != 0`

if and only if some legitimate finite cyclotomic residual image is nonzero.

Thus R5-RES does **not** require a basis theorem over every finite group ring. It requires one protected nonzero finite residual witness whose comparison with the inverse-limit class is governed by the existing transition maps.

This is the operative finite-detection interface.

## Part B — the odd-prime finite witness architecture

The protected source audit identifies the strongest explicit witness route currently admitted.

Chan-Ho Kim's refined reciprocity theorem is stated for `p>=5` and, in the source normalization, proves an identity of the form

`xi o exp* o loc(kappa_n^Kato) = u * p^t * delta_tilde_n`,

where:

- `u` is a `p`-adic unit;
- `p^t = #E(Q_p)[p^infinity]`;
- `delta_tilde_n` is a finite Kurihara/modular-symbol quantity;
- `xi` is built only after computing the integral dual-exponential image and extending the map to torsion coefficients.

The theorem also retains a period/Manin normalization.

For odd `p` this gives a concrete route from an analytic finite quantity to a Kato-derived Kolyvagin component.

## Part C — why the admitted source cannot yet be used at literal 2

There is no admitted theorem identifying the protected literal-2 derivative component with the odd-prime source quantity by direct specialization.

Three distinct issues must be retained.

### C.1 Integral local regulator at `2`

The protected local-control theorems determine finite defects and support, but they do not construct the torsion-coefficient Bloch–Kato dual-exponential map used in Kim's refined reciprocity theorem or compute its exact integral image at literal `2`.

### C.2 Local `2^t` factor

Put

`t_2 = length_Z2(E(Q_2)[2^infinity])`.

The odd-prime source formula tracks the corresponding local factor explicitly. A literal-2 proof must therefore retain `2^t_2` rather than assume it is a unit.

If `t_2>0`, the raw product `2^t_2 * delta_tilde_n` is zero after naive reduction modulo `2`; a nonzero raw Kurihara residue cannot then be promoted through the source formula without an additional integral normalization theorem.

If `t_2=0`, this particular factor is a unit, but the literal-2 integral regulator and period normalization are still not supplied by the source theorem.

No claim is made here about the value of `t_2` for a selected curve.

### C.3 Period/Manin factor

The source theorem assumes a `p`-indivisible Manin constant in the relevant formulation. No protected BSD-001 theorem currently authorizes deleting a possible even period/Manin normalization at `p=2`.

## Part D — exact first missing theorem

The finite-detection route becomes composable if one proves:

`MISSING_P2_NORMALIZED_ANOMALOUS_ORDINARY_KATO_KURIHARA_RECIPROCITY`.

For BSD-001 this theorem must provide all of the following.

1. A literal-2 torsion-coefficient local regulator / dual-exponential map compatible with the selected WP60T Kato derivatives.
2. The exact integral image lattice of that map.
3. Explicit retention of `t_2 = length(E(Q_2)[2^infinity])`.
4. Explicit retention or proof of unitness of every period/Manin normalization entering the modular-symbol comparison.
5. A finite derivative reciprocity identity whose remaining comparison factor is a `2`-adic unit after the explicitly retained factors are removed.
6. Compatibility with the protected finite cyclotomic transition/detection map used by R5-PRIM.

Only after this theorem is available may a nonzero normalized Kurihara/modular-symbol quantity be used to infer that the protected finite Kato/Kolyvagin component is nonzero.

## Part E — disposition

The protected sources and predecessor theorems do not currently prove the theorem in Part D.

Therefore the governed R5-RES candidate disposition is

`BLOCKED`

on

`MISSING_P2_NORMALIZED_ANOMALOUS_ORDINARY_KATO_KURIHARA_RECIPROCITY`.

This is an evidentiary boundary, not a failure of the already-protected global Kolyvagin-system machinery.

## Corollary — reopening condition

R5-RES may be reopened by exact protected evidence that either:

- proves the Part D reciprocity theorem and supplies a nonzero normalized finite witness; or
- proves the residual Kato/Kolyvagin nonvanishing directly by another source-admitted literal-2 route that bypasses the Kurihara comparison.

The present theorem does not establish `R5_RES_ESTABLISHED` or `R5_PRIM_ESTABLISHED`.