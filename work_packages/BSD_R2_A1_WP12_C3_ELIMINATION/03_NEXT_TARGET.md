# Next analytic target after WP12

## `BSD-R2-A1-S3-DIRECT-LENGTH`

WP12 eliminates the `C3` branch from the selected class. The residual hypothesis is now sharpened from

`E[2] irreducible`

to the theorem

`im(rho_bar_{E,2}) = GL_2(F_2) ~= S3`.

No narrowing of the selected class has occurred; surjectivity is a consequence of the existing hypotheses.

## Immediate recovery path

The first next step is not to invent a new source theorem. Re-audit the already admitted Chao Li mod-2 rank-lowering obstruction with the now-proved automatic surjectivity:

1. separate the residual-surjectivity hypothesis, now discharged by WP12, from every remaining local/Heegner/conductor/Selmer hypothesis in the source;
2. determine whether the WP09 auxiliary-field freedom can satisfy those remaining local hypotheses without changing the selected target;
3. if the obstruction becomes fully applicable, state exactly which standard odd-prime primitivity continuation is ruled out in the whole selected class;
4. if any source hypothesis still fails or remains unproved, name it as the precise route boundary.

## Surviving theorem target

Regardless of the route audit, the selected claim still requires an exact integral theorem determining

`len_Z2 Sha(E/Q)[2^infinity] + sum_{ell|N} ord_2(c_ell)`

from

`ord_2(L'(E,1)/(Omega_E Reg_E))`

with every power of `2` explicit.

The theorem may be obtained through a new p=2 Heegner/Kolyvagin mechanism, an exact direct Selmer-length formula, or the independent Iwasawa lane if WP08 and WP09 debts are both closed.

## Success criterion

The next tranche succeeds only if it either:

- closes another exact source-applicability obligation on the `S3` branch; or
- proves/adopts an exact integral theorem that materially advances the direct-length equality.

`BSD-R2-A1` remains unproved until the direct-length equality itself is established and later certified by MATHCERT.