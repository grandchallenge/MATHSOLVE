# Next analytic targets after WP11

WP11 materially splits `BSD-R2-A1-2PRIMARY-DIRECT-LENGTH` into two residual-image branches.

## Branch A — `C3`

### `BSD-R2-A1-C3-DIRECT-LENGTH`

Known after WP11:

- `L=Q(E[2])` is cyclic cubic and totally real;
- every prime dividing `2N` splits completely in `L`;
- finite-level and 2-infinity Selmer/Sha restriction to `L` is exact onto `C3` invariants;
- the invariant/new decomposition over `L` is integral via `(1+sigma+sigma^2)/3`;
- every Tamagawa 2-valuation is explicit from the even multiplicative exponent `n_ell`.

The remaining theorem must determine

`len_Z2 Sha(E/Q)[2^infinity]`

from the WP00-normalized complex leading term after subtracting the now-explicit Tamagawa valuation, or equivalently provide an exact arithmetic object whose invariant summand has that length.

A cubic-base-change route is admissible only if it determines the norm-zero `C3`-new summand exactly; WP11 removes descent defects but does not make that summand vanish.

## Branch B — `S3`

### `BSD-R2-A1-S3-DIRECT-LENGTH`

The WP10 route barrier remains. A successful theorem must handle the genuinely even-degree residual structure without reusing the odd-prime rank-lowering/primitivity argument mechanically.

The admitted Chao Li obstruction remains limited to its stronger surjective/local-hypothesis subcase; it is not a uniform no-go theorem for the entire `S3` branch.

## Uniform completion criterion

`BSD-R2-A1` is settled only if both branches are covered by exact theorems, or by one theorem that treats them uniformly, with no hidden power of `2`.

## Preferred next research move

Attack `BSD-R2-A1-C3-DIRECT-LENGTH` first. WP11 removes all prime-to-2 descent ambiguity there, making it the cleaner branch. The next high-value search/proof question is whether the cyclic-cubic `C3`-new Selmer/Sha summand can be related to an Artin-twisted rank-zero or rank-one leading term by an exact integral formula.

If no such theorem is admitted or proved in-package, the C3 branch itself reaches a new-mathematics frontier at that Artin-twisted exact-length step.
