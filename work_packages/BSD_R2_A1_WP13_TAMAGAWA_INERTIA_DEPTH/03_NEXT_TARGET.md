# Next analytic target after WP13

## `BSD-R2-A1-S3-TAMAGAWA-SATURATED-LENGTH`

WP12 makes residual surjectivity automatic. WP13 identifies every bad-prime 2-primary Tamagawa contribution with explicit local 2-power representation data.

The remaining direct-length theorem must now work in two local regimes without changing the selected class.

### Regime A — residual conductor unchanged

`N(rho_bar_{E,2})=N`, equivalently every `c_ell` is odd.

Here the Tamagawa sum in the selected valuation is zero. The admitted Chao Li route still requires separate checks at `2`, the auxiliary Heegner field, the chosen level-raising prime, and the relevant minimal 2-Selmer rank. Residual surjectivity alone does not prove those.

### Regime B — residual conductor drops

At least one `c_ell` is even. WP13 proves the dropped primes are exactly these even-Tamagawa primes and records their exact valuations from local inertia depth/component-group data.

A uniform theorem must not discard this regime. It must incorporate the local powers of `2` into the Selmer/Euler/Kolyvagin-system comparison or use a different arithmetic-length mechanism.

## Diagnostic literature boundary

Odd-prime Tamagawa-defect theory explains the expected mechanism: divisibility of a Tamagawa factor makes an Euler-system-derived Kolyvagin system nonprimitive and causes the standard bound to miss local factors. The screened theorem is formulated for odd `p`, not for `p=2`, so it is diagnostic only.

The 2026 modular-form Tamagawa-number-conjecture work likewise imposes residual ramification at conductor primes and is not a uniform weight-two `p=2` theorem for this campaign.

## Success criterion

A successful next theorem must provide one of:

1. an integral `p=2` saturation/primitivity theorem whose defect is exactly the WP13 local Tamagawa length;
2. a direct exact Selmer-length theorem that includes the WP13 local terms without Kolyvagin primitivity;
3. an independent exact leading-term mechanism covering both residual-conductor regimes.

The theorem must retain every power of `2` and compose with the WP00 normalization. `BSD-R2-A1` remains unproved until that global equality is established and later adjudicated by MATHCERT.