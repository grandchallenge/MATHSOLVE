# Codex takeover prompt — BSD-001 WP60

Use this prompt to hand WP60 to a fresh Codex agent. The prompt is deliberately a pointer to protected repository state. It must not substitute for re-fetching that state.

---

CONSTITUTIONAL AUTHORITY REPOSITORY: `grandchallenge/INTELLECT`
TARGET WORK REPOSITORY: `grandchallenge/MATHSOLVE`
PROVIDER REPOSITORY: `grandchallenge/MATHFORGE`
CAMPAIGN: `BSD-001 — Birch-Swinnerton-Dyer selected rank-one 2-primary campaign`
PARENT PROGRAMME OWNER: `grandchallenge/MATHSOLVE#164`
WP60 TRACKER: `grandchallenge/MATHSOLVE#215`
CURRENT FRONTIER POINTER: resolve from protected live state; at issuance, WP59 is protected at `grandchallenge/MATHSOLVE@aae4320432a5510146a5af0bebcfd966e36d7419` and the provider source screen is protected at `grandchallenge/MATHFORGE@c8af223d0e05f999b4977a0dae5b1b5281eff93e`.

Take over continuation of the Grand Challenge `BSD-001` campaign at WP60.

Treat this prompt only as a pointer. Do not rely on conversational history, mutable issue summaries, issuance SHAs after state has advanced, or stale branch/CI/review evidence as mathematical authority.

Before mutation:

1. Re-fetch protected `main` for `grandchallenge/INTELLECT`, `grandchallenge/MATHSOLVE`, and `grandchallenge/MATHFORGE`.
2. From protected MATHSOLVE read, in order:
   - `AGENTS.md`;
   - `handoffs/BSD-001/README.md`;
   - `handoffs/BSD-001/WP60_FRONTIER.md`;
   - `work_packages/BSD_R2_A1_WP60_P2_FRONTIER_RESEARCH_PROGRAM/00_README.md`;
   - `work_packages/BSD_R2_A1_WP60_P2_FRONTIER_RESEARCH_PROGRAM/01_EXECUTION_CONTRACT.md`;
   - `work_packages/BSD_R2_A1_WP60_P2_FRONTIER_RESEARCH_PROGRAM/02_CLAIM_LEDGER.yaml`.
3. Reconstruct the current exact theorem boundary from protected WP59 artifacts rather than from this prompt.
4. Reconcile any protected-state advance before continuing. Reject stale reviews, CI, patches, or source anchors.

The entering substantive residual is

`R_2(E,K,f)=2 ord_2(m_K(f))-ord_2(lambda_D)`

with

`lambda_D=L(E^D,1)/Omega(E^D)`.

At issuance the named D2d boundary is

`MISSING_LITERAL_P2_COMBINED_HEEGNER_INDEX_TWIST_LRATIO_THEOREM_WITHOUT_EXTRA_MOD2_LOG_OR_RANKZERO_SEED`.

The selected theorem remains `BSD-R2-A1 = SELECTED_RESEARCH_TARGET_UNPROVED`.

Execute autonomously under the following priority.

## Priority 1 — WP60A finite-level literal-p=2 analytic determinant generator

Attempt to bypass separate evaluation of the Heegner index and twist L-ratio. Use the already protected primitive finite `2`-Selmer/Fitting, square-presentation, strict/Kummer comparison, Bockstein, local-factor, and normalization machinery to construct an exact integral analytic generator at the height-one prime `(2)`.

Required sequence:

A0. Build an exact dependency map from the current protected WP16–WP19 and WP35–WP52 chain. Identify the precise analytic-generator hole.

A1. Construct or admit an exact finite-level analytic object. Do not invoke a theorem whose integral clause assumes `p>2`.

A2. Match it to the protected primitive Kummer determinant line, accounting exactly for local conditions at `2`, bad primes, primitive/imprimitive changes, exceptional-zero/unit-root terms, torsion, and every power of `2`.

A3. Prove an exact determinant/Fitting equality or preferred-generator statement strong enough to satisfy WP59 reopening condition R5, or directly determine the combined residual as R4.

A4. On apparent success, replay the WP59 reopening contract and the full protected valuation line before claiming D2d reopened or resolved.

## Priority 2 — WP60B Kriz–Li star forcing

Do not begin with another broad Heegner-source search. First normalize the exact literal-p=2 Kriz–Li condition into WP00/WP09/WP38 notation.

Write

`P_K(f)=m_K(f)P+T`

for the fixed protected parametrization and primitive rational generator. Separate fixed curve-local factors, the unknown `m_K(f)` contribution, genuinely `K`-varying data, and differential/logarithmic normalization.

Then classify the condition as:

- `INDEPENDENT_AUXILIARY_NONVANISHING`;
- `EQUIVALENT_TO_UNKNOWN_INDEX_PARITY`; or
- `MIXED`.

If it is equivalent to the unknown index parity, record that strategy reduction and do not claim an independent route.

If independent information remains, prove or source a theorem forcing the exact mod-2 condition for some WP09-compatible auxiliary field while preserving all splitting, discriminant, and nonvanishing conditions. A successful theorem must satisfy WP59 R3 literally. Any material external theorem must be admitted through MATHFORGE first.

## Priority 3 — WP60C exact real-data reconnaissance

Run only on real elliptic curves satisfying all selected hypotheses and real WP09-compatible imaginary quadratic fields.

Use no dummy data.

Produce a deterministic runner, exact input manifest, pinned software/dependency record, machine-readable results, and a written analysis that clearly separates computation from proof.

Compute as available:

- the source-compatible Heegner point;
- the exact normalized `2`-adic logarithm entering the translated Kriz–Li condition;
- independently computable `m_K(f)` values;
- local/formal-group invariants at `2`;
- WP36 finite norm-class quantities;
- Tamagawa valuations;
- exact modular-symbol/twist-L-ratio data where available;
- the truth value of the translated mod-2 condition.

Use computation to falsify conjectures, identify finite local predictors, or formulate a theorem narrow enough to prove. Never promote finite data to a uniform theorem.

## Parallel live boundaries

D1c: `MISSING_P2_ANALYTIC_DETERMINANT_GENERATOR_AT_HEIGHT_ONE_2`.

D2a: `MISSING_P2_K_HEIGHT_NONDEGENERACY`.

D2e remains downstream and must not be replayed as resolved until D2d is genuinely on an exact protected line.

## Execution doctrine

Continue autonomously through all reasonably available bounded proof, falsification, source admission, computation, repair, replay, evidence gathering, exact-head non-authoring Adversary and Referee passes, ordinary CI, protected merge, protected readback, issue maintenance, and handoff maintenance.

Recoverable infrastructure, connector, compiler, Lean, Sage/PARI, CI, logging, or environment failures are recovery events, not stopping conditions.

Where exact identity matters, bind every action to the current exact head/run/job/artifact and reject stale evidence. After every repair, rerun the affected exact-head checks and reviews.

Do not:

- write directly to protected branches;
- manufacture Human Steward, Referee, MATHCERT, or other reserved authority;
- infer certification from CI;
- use an odd-prime theorem at literal `p=2` without a theorem that actually includes `p=2`;
- set `m_K(f)` odd or `lambda_D` a `2`-unit without proof;
- weaken WP09 local constraints;
- treat computational correlation as proof;
- repeat the WP59 broad rejected-route sweep without materially new mathematics.

Routine protected merges do not require a new Human Steward approval under the current delegated maintenance authority, provided all governing exact-head controls are satisfied.

Before stopping, name the exact theorem/source/authority/authentication/safety/material-state/evidentiary boundary that prevents the next bounded recovery or proof action. If you cannot name such a boundary, continue.

---
