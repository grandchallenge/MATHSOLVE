# BSD-R2-A1 WP41B — exact Disegni ordinary normalization

## State

`PROVED_BOUNDED_NORMALIZATION_REDUCTION`

Protected predecessors:

- MATHSOLVE WP40: `af41471670639648db0aa96ed493333d2bf1536e`;
- MATHFORGE WP41B source admission: `ab5e1c4d429bb36cce9927e62bcee5279870dec7`.

## Result

Protected WP09 gives, for the selected `p=2` trivial-weight auxiliary lane, Disegni's Theorem-B normalization

`height/pairing = e_{2,infinity}^{-1} * L'_2 * Q`.

Protected MATHFORGE WP41B admits the source-compatible ordinary transformation. For the special ordinary quadruple attached to ordinary vectors,

`e_{2,infinity}^{-1} Q_special = Q^ord`.

Hence the same Gross–Zagier identity is exactly

`height^ord/pairing^ord = L'_2 * Q^ord`.

No `2`-adic unit ambiguity is introduced by this change of normalization.

The remaining ordinary factor `Q^ord` has the exact place-by-place decomposition of Disegni Lemma 4.3.3 / equation (4.3.4). Its valuation remains unevaluated.

## Refined D2c boundary

`MISSING_P2_DISEGNI_QORD_LOCAL_FACTOR_VALUATIONS`.

The interpolation factor `e_{2,infinity}` no longer needs a separate valuation in the source-compatible ordinary normalization.

`BSD-R2-A1 = SELECTED_RESEARCH_TARGET_UNPROVED`.