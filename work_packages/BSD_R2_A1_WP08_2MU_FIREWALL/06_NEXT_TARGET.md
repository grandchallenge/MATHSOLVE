# Next analytic target

## `BSD-R2-A1-2MU-RECIPROCITY`

WP08 has reduced the surviving theorem debt to two explicit, independent components.

### A. `(2)`-height / mu component

Supply an integral theorem under the selected hypotheses that determines the height-one `(2)` component of the relevant cyclotomic or Heegner characteristic relation.

For a main-conjecture route this may take the form

`mu_alg = mu_an`

combined with equality away from `(2)`, or a stronger integral characteristic/Fitting equality directly.

A theorem proving both mu invariants zero is sufficient but not necessary; equality at a nonzero common value also removes the **relative** `2^delta` defect.

### B. rank-one reciprocity component

Supply an exact rank-one theorem connecting the relevant p-adic/Heegner leading object to

`L'(E,1)/(Omega_E Reg_E)`

with the WP07 unit-root correction and every WP00 Tamagawa, period, isogeny, Manin, Euler, and height normalization retained.

### Direct arithmetic bypass

An integral p=2 Kolyvagin/Heegner-index theorem that computes

`len_Z2 Sha(E/Q)[2^infinity] + sum_{ell|N} ord_2(c_ell)`

directly from the WP00 complex leading term may bypass the characteristic-ideal/mu presentation. Such a route is acceptable only if it accounts for every 2-primary correction rather than hiding them in a unit or an unspecified power of 2.

### Stop rule

No further descent, local-normalization, or divisor bookkeeping remains before this target. If no admitted theorem supplies A and B, the next step is genuinely new integral p=2 Iwasawa/reciprocity mathematics.