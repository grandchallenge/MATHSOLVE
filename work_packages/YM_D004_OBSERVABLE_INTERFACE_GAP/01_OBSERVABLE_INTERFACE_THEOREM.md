# YM-D004-R001-L001 — observable-interface noncomposition theorem

## Statement

Let `T_AF` denote protected theorem interface `YM-T-100` and `T_OS` protected interface `YM-T-120`.

Let `O_D004` denote the D004 target object: a renormalized local gauge-invariant continuum operator hierarchy corresponding to the intended curvature-polynomial classes, with short-distance correlation asymptotics normalized against the admitted perturbative ultraviolet data.

The current protected records imply all of the following.

### A. Perturbative ultraviolet data do not construct `O_D004`

`T_AF` determines the asymptotic-freedom sign of the leading beta function within perturbation theory and the source renormalization convention.

Its protected conclusion contains no construction of a nonperturbative continuum measure, no local quantum operator hierarchy, and no convergence theorem for regulated gauge-invariant observables.

Therefore `T_AF` alone does not construct `O_D004`.

### B. OS reconstruction presupposes the relevant observable hierarchy

`T_OS` is quantified over a continuum Schwinger hierarchy satisfying the complete selected OS profile. Its explicit hypotheses include a physical gauge-invariant observable hierarchy.

Therefore `O_D004`, or an adequate hierarchy containing it, is input-side data for `T_OS`; it is not generated by the reconstruction conclusion.

### C. Sequential composition does not close D004

There is no typed composition

`T_AF ; T_OS -> O_D004`

in the protected interfaces.

The output of `T_AF` is perturbative ultraviolet running data, not the continuum Schwinger/observable hierarchy required by `T_OS`. The output of `T_OS` is a reconstructed Hilbert-space QFT conditional on the hierarchy; it cannot retroactively prove existence of an input it assumed.

Thus the pair cannot discharge D004 without an additional theorem constructing and identifying the renormalized local observable hierarchy.

### D. The missing interface is a renormalized-observable construction theorem

For a selected regulated route, a D004 successor must supply a mathematically controlled map, possibly including operator mixing,

[
  mathcal O^{mathrm{reg}}_{a}
  longrightarrow
  mathcal O^{mathrm{ren}},
]

with enough convergence and identification structure to produce local gauge-invariant continuum operator-valued distributions and their joint correlations.

The successor must also prove that the relevant short-distance correlations have the required perturbative normalization/asymptotics. The exact analytic implementation is route-dependent and is not assumed here.

## Proof

### Step 1 — type `YM-T-100`

The Programme ledger records `YM-T-100` with direction `ultraviolet_beta_function`. Its conclusion is the sign of the leading beta function corresponding to asymptotic freedom within perturbation theory.

Its claim boundary explicitly excludes construction of the nonperturbative measure. A fortiori, the protected interface does not output a nonperturbative renormalized local observable hierarchy.

Hence `T_AF` has the wrong codomain for D004 construction.

### Step 2 — type `YM-T-120`

The Programme ledger records `YM-T-120` with direction `euclidean_to_physical_qft`. Its hypotheses include the physical gauge-invariant observable hierarchy, alongside the other selected OS conditions.

Its conclusion is reconstruction of a positive Hilbert-space QFT with translations and Hamiltonian.

Therefore the observable hierarchy occurs on the domain side of this theorem interface.

### Step 3 — apply the governing target requirement

The protected WP00 normalization audit, following the official Jaffe–Witten problem statement, requires local quantum fields corresponding to gauge-invariant local curvature polynomials and the required short-distance perturbative behavior.

The official source separately warns that the classical-to-quantum correspondence has renormalization subtleties rather than a natural one-to-one identification.

Therefore merely naming a classical curvature polynomial, or reconstructing a QFT from an already-given hierarchy, does not establish the required quantized local observable.

### Step 4 — reject the two possible circular substitutions

First substitution: infer the observable hierarchy from `T_OS`.

This is circular because the hierarchy is among `T_OS`'s hypotheses.

Second substitution: infer the hierarchy from `T_AF`.

This is a type error because `T_AF` supplies perturbative running data, not existence/convergence/identification of nonperturbative operator-valued distributions.

No protected theorem currently fills either mismatch.

### Step 5 — identify the minimal residual

The missing object is consequently a theorem that begins with a selected regulated gauge-invariant observable family and constructs a renormalized continuum local observable hierarchy on the same limiting theory, with short-distance normalization tied to the perturbative interface.

This proves A–D.

## FP1 — beta-function substitution

A perturbative theorem may determine a running coupling or anomalous-dimension coefficient without defining any nonperturbative random field or operator-valued distribution.

Therefore perturbative ultraviolet running is insufficient by itself to prove existence of a continuum local observable.

## FP2 — reconstruction circularity

Suppose theorem R states: every hierarchy satisfying hypotheses H, including property O, reconstructs an object Q.

From R alone one cannot infer O. Doing so uses a premise of R as though it were a conclusion.

This is the exact prohibited use of `YM-T-120` for D004 observable construction.

## FP3 — classical-label substitution

Even when a classical local polynomial such as a curvature invariant is unambiguous, quantization can require renormalization and operator mixing. Naming the classical polynomial therefore does not by itself identify a unique continuum quantum operator.

This fixture is not a claim about a particular renormalization scheme; it enforces the governing source's explicit renormalization boundary.

## Consequence for the campaign

The D004 frontier is sharpened to:

`YM-D004-R002 — REGULATED_TO_RENORMALIZED_LOCAL_OBSERVABLE_CONSTRUCTION`.

Only after such an interface exists can a later tranche test its full short-distance concordance and its compatibility with the selected OS/continuum construction.

## Claim boundary

This theorem establishes only a dependency and noncomposition result.

It does not construct `mathcal O^{ren}`, prove convergence of any regulated Yang-Mills observable, prove the complete OS profile, establish a nontrivial four-dimensional continuum theory, or establish the mass gap.
