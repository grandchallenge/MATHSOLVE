# BSD-R2-A1 WP60F — BSS literal-p=2 proof boundaries

## Purpose

Propagate the protected MATHFORGE WP60F proof-dependency audit into the mathematical execution frontier for R5.

This package does not prove or disprove a literal-`p=2` extension of the Burns–Sakamoto–Sano machinery. It records exactly what must be replaced before that architecture can yield the protected integral Fitting conclusion.

## Protected inputs

- MATHSOLVE base: `c2f036fd935238a880780685a927c26ec0de2d4e`.
- MATHFORGE WP60F: `eaaf7b8c660f3f07030608b3af1334ece5598586`.
- Programme tracker: `grandchallenge/MATHSOLVE#215`.
- Programme owner: `grandchallenge/MATHSOLVE#164`.
- Selected target: `BSD-R2-A1 = SELECTED_RESEARCH_TARGET_UNPROVED`.

Protected WP60A-A1 leaves:

- `R5-LIFT = MISSING_P2_KATO_ZETA_FITTING_DIVISIBILITY_AT_HEIGHT_ONE_2`;
- `R5-PRIM = MISSING_P2_DETERMINANTAL_ZETA_PRIMITIVITY_AT_HEIGHT_ONE_2`.

Protected WP60E additionally records

`MISSING_LITERAL_P2_FINITE_LEVEL_KATO_DERIVATIVE_FITTING_THEOREM`.

## WP60F result

The BSS route splits into two independent literal-`2` obligations.

### F1 — foundational Kolyvagin/Fitting control

`MISSING_P2_CORE_VERTEX_SIMULTANEOUS_LOCALIZATION_FOR_BSS_FITTING_CONTROL`.

The admitted BSS core-vertex and Kolyvagin-system control chain uses simultaneous localization with `s+t<p` and connectivity with `2s<p`; the Fitting-control theorems are proved under `p>3`.

### F2 — elliptic standard-hypothesis verification

`MISSING_P2_ELLIPTIC_H2_H3_VERIFICATION_OVER_F2_INFINITY`.

The admitted elliptic application proves its H2/H3 hypotheses from a large `p`-adic image by a `p>3` perfectness argument after restriction to the relevant pro-`p` extension. Protected residual surjectivity of `E[2]` is not an admitted replacement for that argument.

## Exact disposition

`BSS_LITERAL_P2_R5_ROUTE_REQUIRES_F1_AND_F2`.

Repairing only F1 does not verify the elliptic hypotheses. Repairing only F2 does not supply the literal-`2` Kolyvagin/Fitting-control theorem.

The formal existence of a higher derivative expression is not, by itself, Fitting control.

## Reopening forms

The BSS route reopens on a coherent package that supplies both required layers, or bypasses them with a direct literal-`2` theorem. Admissible forms include:

1. a literal-`2` simultaneous-localization/core-vertex theorem strong enough to recover the required part of BSS Theorem 5.20/Theorem 5.2;
2. a direct literal-`2` Kolyvagin-system/Fitting theorem avoiding the `p>3` connectivity argument;
3. a selected-curve proof of the exact BSS H2/H3 hypotheses over the relevant `2`-power extension;
4. an elliptic finite-level Fitting theorem whose hypotheses are directly implied by the protected selected class;
5. a direct primitive finite Kummer/Fitting argument satisfying WP60E reopening form E3.

## Next research action

Attack F1 first. It is upstream of the Fitting conclusion and remains a blocker even if F2 is solved. The next bounded tranche should determine whether the `s+t<p` Chebotarev condition can be replaced at `p=2` under the protected selected representation, or whether the same obstruction persists in any core-vertex proof of the needed Fitting inclusion.

F2 may be investigated in parallel only with exact group-theoretic control of the restricted `2`-adic image; residual surjectivity alone is insufficient.

## Claim firewall

Do not infer:

- a literal-`p=2` BSS Fitting theorem;
- failure of higher Kolyvagin derivatives at `p=2`;
- failure of H2/H3 for every selected curve;
- `R5-LIFT`, `R5-PRIM`, D2d, or `BSD-R2-A1`;
- MATHCERT certification, novelty, or priority.
