# WP60J theorem — selected residual BSS Hypothesis 3.2 at literal `p=2`

## 1. Protected setup

Work in the selected `BSD-R2-A1` class. Thus `E/Q` is semistable, has odd conductor, good ordinary reduction at `2`, irreducible `E[2]`, and analytic rank one.

Protected inputs used here are:

- WP12: `im(rho_bar_{E,2})=GL_2(F_2)~=S3`;
- WP13: at an odd multiplicative prime `ell`, inertia is `[[1,n_ell t(sigma)],[0,1]]`, with `t` surjective;
- WP60I: some odd bad `ell` has odd `n_ell`, hence the restricted 2-adic image contains the exact primitive unipotent `U=[[1,1],[0,1]]`;
- protected MATHFORGE WP60G: the exact BSS II Hypothesis 3.2 / Lemma 3.9 interface.

The source definition for a finite coefficient ring `R` sets `M=min{2^n:2^n R=0}` and

`K_M=K(mu_M,(O_K^x)^(1/M))K(1)`,

`K(A)_M=K(A)K_M`.

For the residual specialization `K=Q`, `R=k=F_2`, `A=E[2]`, one has `M=2` and

`K_M=Q(i)`.

The last equality follows because `Q` has trivial Hilbert-class contribution, `mu_2` is rational, and adjoining square roots of the units `+-1` adds exactly `i`.

## 2. Primitive transvection forces the full 2-adic image

### Theorem `BSD-A1-WP60J-FULL-IMAGE-001`

For every selected curve,

`im(rho_{E,2-adic})=GL_2(Z_2)`.

### Proof

By WP60I there is an odd bad prime `ell` and an inertia element whose 2-adic image, in a suitable basis, is

`U=I+E_12=[[1,1],[0,1]]`.

Let `Gamma` be the full 2-adic image. WP12 gives `Gamma mod 2=GL_2(F_2)`.

Choose `g in Gamma` such that, modulo `2`, `g` does not preserve the fixed line of `U`. Put `v=g e_1`. Then `(e_1,v)` is a `Z_2`-basis. In this basis, `U` fixes `e_1` and sends

`v -> v+b e_1`

for a unit `b in Z_2^x`, because the corresponding coefficient is nonzero modulo `2`. Hence

`U=[[1,b],[0,1]]`.

The conjugate `gUg^(-1)` fixes `v`; its nilpotent part has image and kernel equal to the line `Z_2 v`. Since `e_1` is not fixed modulo `2`, in the same basis

`gUg^(-1)=[[1,0],[a,1]]`

with `a in Z_2^x`.

For integers `m`, nilpotence of the off-diagonal matrix gives

`[[1,b],[0,1]]^m=[[1,mb],[0,1]]`,

and similarly in the lower-triangular direction. Taking closures and using that `a,b` are units shows that `Gamma` contains

`[[1,x],[0,1]]` and `[[1,0],[y,1]]`

for every `x,y in Z_2`.

Upper and lower elementary matrices generate `SL_2(Z_2)`. Indeed, Gaussian elimination over the local ring `Z_2` reduces any determinant-one matrix using these elementary operations; if the upper-left entry is not a unit, the lower-left entry is a unit and an elementary Weyl move interchanges the two rows/columns before elimination. Thus

`SL_2(Z_2) subset Gamma`.

The determinant of the elliptic 2-adic representation is the 2-adic cyclotomic character, whose image over `Q` is all `Z_2^x`. Hence `det(Gamma)=Z_2^x`. Since `Gamma` contains the determinant kernel `SL_2(Z_2)`, it follows that

`Gamma=GL_2(Z_2)`.

QED.

## 3. The residual auxiliary field is a direct product

Put `L=Q(E[2])`. WP12 gives `Gal(L/Q)=S3`.

### Lemma `BSD-A1-WP60J-DISJOINT-002`

`L intersect Q(i)=Q`.

### Proof

WP60I supplies an odd bad prime `ell` with odd inertia depth. By WP13 the residual inertia group at `ell` is generated by a transposition in `S3`.

If `L intersect Q(i)` were nontrivial, it would equal `Q(i)`, since `Q(i)` is quadratic. The unique quadratic subfield of an `S3`-extension is the fixed field of `A3`. A transposition maps nontrivially to `S3/A3`, so that quadratic subfield is ramified at `ell`.

But `Q(i)/Q` is unramified at every odd prime. Contradiction.

QED.

Therefore

`Gal(K(A)_2/Q)=Gal(LQ(i)/Q)=S3 x C2`.

The `C2` factor acts trivially on `A=E[2]`.

## 4. Residual cohomology vanishing

Let `V=E[2]~=F_2^2` with the natural `S3=GL_2(F_2)` action.

### Lemma `BSD-A1-WP60J-H1-003`

`H^1(S3,V)=0` and `V^S3=0`.

### Proof

The invariants vanish because `V` is the nontrivial irreducible two-dimensional `F_2[S3]`-module.

For cohomology, use the normal subgroup `C3=A3`. An element of order three acts on `V` with characteristic polynomial `x^2+x+1`, so `V^C3=0`. Since `3` is invertible in `F_2`, `H^1(C3,V)=0`. Inflation-restriction for

`1 -> C3 -> S3 -> C2 -> 1`

then gives `H^1(S3,V)=0`.

QED.

### Lemma `BSD-A1-WP60J-H1-PRODUCT-004`

`H^1(S3 x C2,V)=0`.

### Proof

Apply inflation-restriction to

`1 -> C2 -> S3 x C2 -> S3 -> 1`,

where the kernel acts trivially on `V`. The relevant exact sequence begins

`0 -> H^1(S3,V) -> H^1(S3 x C2,V) -> Hom(C2,V)^S3`.

The left term is zero by the preceding lemma. The right term is `V^S3`, also zero. Therefore the middle term vanishes.

QED.

The executable certificate `02_RESIDUAL_COHOMOLOGY_CERTIFICATE.py` independently exhausts the finite groups and verifies that every 1-cocycle is a coboundary.

By Weil self-duality, `E[2]~=E[2]^*(1)`, so the same vanishing holds for the dual coefficient module.

## 5. Verification of BSS Hypotheses 3.2 and 3.3

### Theorem `BSD-A1-WP60J-BSS-RESIDUAL-005`

For the selected residual specialization

`K=Q`, `R=k=F_2`, `A=E[2]`,

BSS II Hypotheses 3.2 and 3.3 hold at literal `p=2`.

### Proof

We verify the three clauses of BSS Hypothesis 3.2.

1. **3.2(i), irreducibility.** This is protected WP12.

2. **3.2(ii), rank-one quotient.** Choose the odd bad prime `ell` and primitive inertia element `tau` from WP60I. Since `ell` is odd, inertia at `ell` fixes `Q(i)=K_2`, so `tau in G_{K_2}`. On `E[2]`, `tau` is the nontrivial unipotent matrix. Therefore

   `A/(tau-1)A ~= F_2 = R`.

3. **3.2(iii), finite residual H1 vanishing.** By Lemma `BSD-A1-WP60J-DISJOINT-002`,

   `Gal(K(A)_2/Q)=S3 x C2`.

   Lemma `BSD-A1-WP60J-H1-PRODUCT-004` gives

   `H^1(K(A)_2/Q,A)=0`.

   Weil self-duality gives the identical assertion for `A^*(1)`.

BSS Hypothesis 3.3 asks for zero global invariants of the residual primal and dual modules. This follows from irreducibility/nontriviality of `E[2]` and Weil self-duality.

QED.

## 6. Consequence for WP60G and WP60H

Protected MATHFORGE WP60G records that BSS Hypothesis 3.2(iii) is used in Lemma 3.9 to make the finite residual restriction map injective before the affine-fiber argument. Theorem `BSD-A1-WP60J-BSS-RESIDUAL-005` discharges that hypothesis for the selected residual module.

Therefore the protected MATHSOLVE WP60G self-dual pairwise localization theorem and WP60H odd-relation/four-fiber reduction no longer carry an unresolved selected-lane residual-Hypothesis-3.2 conditionality.

Record

`WP60G_WP60H_RESIDUAL_HYP32_CONDITIONALITY_DISCHARGED`.

The remaining residual F1 frontier is still exactly

`MISSING_P2_BSS_MINIMAL_CORE_THREE_TERM_RELATION_EXCLUSION_OR_REPLACEMENT_CONNECTIVITY`.

## 7. Why this does not contradict WP60I

WP60I proves that the BSS III **infinite** condition `(H3)` fails for the Tate module: the group

`H^1(Q(E[2^infinity])/Q,E[2])`

is nonzero.

The finite residual BSS II Hypothesis 3.2(iii) used above is instead

`H^1(Q(E[2])Q(i)/Q,E[2])=0`.

These are different Galois groups. The higher 2-power division tower creates the WP60I obstruction; it is absent at the residual finite level.

Hence both protected conclusions coexist:

`BSS_LITERAL_P2_SELECTED_RESIDUAL_HYP32_HYP33_HOLD`

and

`BSS_LITERAL_P2_SELECTED_H2_HOLDS_H3_FAILS`.

The first repairs the residual localization interface. The second still blocks the currently screened standard integral BSS application theorem.

## 8. Claim firewall

WP60J does not establish:

- the WP60H three-term-relation exclusion;
- literal-2 BSS Lemma 5.15 or Corollary 5.16 in full;
- BSS Theorem 5.20, Theorem 5.2, Theorem 5.25, or Corollary 6.15 at `p=2`;
- vanishing of higher-level or infinite H3;
- R5-LIFT, R5-PRIM, D2d, or `BSD-R2-A1`;
- MATHCERT certification, novelty, or priority.
